//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommBasicObjects/CommMobileUltrasonicScanCore.hh"

// serialization/deserialization operators
//#include "CommBasicObjects/CommMobileUltrasonicScanACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommBasicObjects 
{
	const char* CommMobileUltrasonicScanCore::getCompiledHash()
	{
		return CommBasicObjectsIDL::REPO_HASH;
	}
	
	void CommMobileUltrasonicScanCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommBasicObjects::CommBaseState(idl_CommMobileUltrasonicScan.baseState)
		CommBasicObjects::CommBaseState::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommUltrasonicScan(idl_CommMobileUltrasonicScan.usScan)
		CommBasicObjects::CommUltrasonicScan::getAllHashValues(hashes);
	}
	
	void CommMobileUltrasonicScanCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommMobileUltrasonicScanCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommBasicObjects::CommBaseState(idl_CommMobileUltrasonicScan.baseState)
		CommBasicObjects::CommBaseState::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommUltrasonicScan(idl_CommMobileUltrasonicScan.usScan)
		CommBasicObjects::CommUltrasonicScan::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommMobileUltrasonicScanCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += CommBasicObjects::CommBaseState::generateDataHash(data.baseState);
		seed += CommBasicObjects::CommUltrasonicScan::generateDataHash(data.usScan);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommMobileUltrasonicScanCore::CommMobileUltrasonicScanCore()
	:	idl_CommMobileUltrasonicScan()
	{  
		setBaseState(CommBasicObjects::CommBaseState());
		setUsScan(CommBasicObjects::CommUltrasonicScan());
	}
	
	CommMobileUltrasonicScanCore::CommMobileUltrasonicScanCore(const DATATYPE &data)
	:	idl_CommMobileUltrasonicScan(data)
	{  }

	CommMobileUltrasonicScanCore::~CommMobileUltrasonicScanCore()
	{  }
	
	void CommMobileUltrasonicScanCore::to_ostream(std::ostream &os) const
	{
	  os << "CommMobileUltrasonicScan(";
	  getBaseState().to_ostream(os);
	  getUsScan().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void CommMobileUltrasonicScanCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<baseState>";
		getBaseState().to_xml(os, indent);
		os << indent << "</baseState>";
		os << indent << "<usScan>";
		getUsScan().to_xml(os, indent);
		os << indent << "</usScan>";
	}
	
	// restore from xml stream
	void CommMobileUltrasonicScanCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_baseState("<baseState>");
		static const Smart::KnuthMorrisPratt kmp_usScan("<usScan>");
		
		if(kmp_baseState.search(is)) {
			CommBasicObjects::CommBaseState baseStateItem;
			baseStateItem.from_xml(is);
			setBaseState(baseStateItem);
		}
		if(kmp_usScan.search(is)) {
			CommBasicObjects::CommUltrasonicScan usScanItem;
			usScanItem.from_xml(is);
			setUsScan(usScanItem);
		}
	}
	
	/*
	void CommMobileUltrasonicScanCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommBasicObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommBasicObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommMobileUltrasonicScanACE.hh)
		cdr << idl_CommMobileUltrasonicScan;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommMobileUltrasonicScan);
		cdr << data_hash;
		// std::cout << "CommMobileUltrasonicScanCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommMobileUltrasonicScanCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommBasicObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommMobileUltrasonicScanACE.hh)
		cdr >> idl_CommMobileUltrasonicScan;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommMobileUltrasonicScan);
		assert(data_hash == own_hash);
		// std::cout << "CommMobileUltrasonicScanCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommBasicObjects */
