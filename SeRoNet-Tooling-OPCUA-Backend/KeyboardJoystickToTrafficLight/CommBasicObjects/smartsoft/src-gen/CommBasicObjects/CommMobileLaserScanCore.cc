//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommBasicObjects/CommMobileLaserScanCore.hh"

// serialization/deserialization operators
//#include "CommBasicObjects/CommMobileLaserScanACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommBasicObjects 
{
	const char* CommMobileLaserScanCore::getCompiledHash()
	{
		return CommBasicObjectsIDL::REPO_HASH;
	}
	
	void CommMobileLaserScanCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommBasicObjects::CommBaseState(idl_CommMobileLaserScan.base_state)
		CommBasicObjects::CommBaseState::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::LaserScan6DPose(idl_CommMobileLaserScan.laser_scan_pose)
		CommBasicObjects::LaserScan6DPose::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommPose3d(idl_CommMobileLaserScan.laser_world_pose)
		CommBasicObjects::CommPose3d::getAllHashValues(hashes);
	}
	
	void CommMobileLaserScanCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommMobileLaserScanCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommBasicObjects::CommBaseState(idl_CommMobileLaserScan.base_state)
		CommBasicObjects::CommBaseState::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::LaserScan6DPose(idl_CommMobileLaserScan.laser_scan_pose)
		CommBasicObjects::LaserScan6DPose::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommPose3d(idl_CommMobileLaserScan.laser_world_pose)
		CommBasicObjects::CommPose3d::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommMobileLaserScanCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += CommBasicObjects::CommBaseState::generateDataHash(data.base_state);
		seed += CommBasicObjects::LaserScan6DPose::generateDataHash(data.laser_scan_pose);
		seed += CommBasicObjects::CommPose3d::generateDataHash(data.laser_world_pose);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommMobileLaserScanCore::CommMobileLaserScanCore()
	:	idl_CommMobileLaserScan()
	{  
		setBase_state(CommBasicObjects::CommBaseState());
		setLaser_scan_pose(CommBasicObjects::LaserScan6DPose());
		setLaser_world_pose(CommBasicObjects::CommPose3d());
	}
	
	CommMobileLaserScanCore::CommMobileLaserScanCore(const DATATYPE &data)
	:	idl_CommMobileLaserScan(data)
	{  }

	CommMobileLaserScanCore::~CommMobileLaserScanCore()
	{  }
	
	void CommMobileLaserScanCore::to_ostream(std::ostream &os) const
	{
	  os << "CommMobileLaserScan(";
	  getBase_state().to_ostream(os);
	  getLaser_scan_pose().to_ostream(os);
	  getLaser_world_pose().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void CommMobileLaserScanCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<base_state>";
		getBase_state().to_xml(os, indent);
		os << indent << "</base_state>";
		os << indent << "<laser_scan_pose>";
		getLaser_scan_pose().to_xml(os, indent);
		os << indent << "</laser_scan_pose>";
		os << indent << "<laser_world_pose>";
		getLaser_world_pose().to_xml(os, indent);
		os << indent << "</laser_world_pose>";
	}
	
	// restore from xml stream
	void CommMobileLaserScanCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_base_state("<base_state>");
		static const Smart::KnuthMorrisPratt kmp_laser_scan_pose("<laser_scan_pose>");
		static const Smart::KnuthMorrisPratt kmp_laser_world_pose("<laser_world_pose>");
		
		if(kmp_base_state.search(is)) {
			CommBasicObjects::CommBaseState base_stateItem;
			base_stateItem.from_xml(is);
			setBase_state(base_stateItem);
		}
		if(kmp_laser_scan_pose.search(is)) {
			CommBasicObjects::LaserScan6DPose laser_scan_poseItem;
			laser_scan_poseItem.from_xml(is);
			setLaser_scan_pose(laser_scan_poseItem);
		}
		if(kmp_laser_world_pose.search(is)) {
			CommBasicObjects::CommPose3d laser_world_poseItem;
			laser_world_poseItem.from_xml(is);
			setLaser_world_pose(laser_world_poseItem);
		}
	}
	
	/*
	void CommMobileLaserScanCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommBasicObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommBasicObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommMobileLaserScanACE.hh)
		cdr << idl_CommMobileLaserScan;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommMobileLaserScan);
		cdr << data_hash;
		// std::cout << "CommMobileLaserScanCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommMobileLaserScanCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommBasicObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommMobileLaserScanACE.hh)
		cdr >> idl_CommMobileLaserScan;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommMobileLaserScan);
		assert(data_hash == own_hash);
		// std::cout << "CommMobileLaserScanCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommBasicObjects */
