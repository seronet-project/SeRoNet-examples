//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommBasicObjects/LaserScan6DPoseCore.hh"

// serialization/deserialization operators
//#include "CommBasicObjects/LaserScan6DPoseACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommBasicObjects 
{
	const char* LaserScan6DPoseCore::getCompiledHash()
	{
		return CommBasicObjectsIDL::REPO_HASH;
	}
	
	void LaserScan6DPoseCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommBasicObjects::CommLaserScan(idl_LaserScan6DPose.laser_scan)
		CommBasicObjects::CommLaserScan::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommPose3d(idl_LaserScan6DPose.sensor_pose)
		CommBasicObjects::CommPose3d::getAllHashValues(hashes);
	}
	
	void LaserScan6DPoseCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "LaserScan6DPoseCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommBasicObjects::CommLaserScan(idl_LaserScan6DPose.laser_scan)
		CommBasicObjects::CommLaserScan::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommPose3d(idl_LaserScan6DPose.sensor_pose)
		CommBasicObjects::CommPose3d::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t LaserScan6DPoseCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += CommBasicObjects::CommLaserScan::generateDataHash(data.laser_scan);
		seed += CommBasicObjects::CommPose3d::generateDataHash(data.sensor_pose);
		
		return seed;
	}
	#endif
	
	// default constructor
	LaserScan6DPoseCore::LaserScan6DPoseCore()
	:	idl_LaserScan6DPose()
	{  
		setLaser_scan(CommBasicObjects::CommLaserScan());
		setSensor_pose(CommBasicObjects::CommPose3d());
	}
	
	LaserScan6DPoseCore::LaserScan6DPoseCore(const DATATYPE &data)
	:	idl_LaserScan6DPose(data)
	{  }

	LaserScan6DPoseCore::~LaserScan6DPoseCore()
	{  }
	
	void LaserScan6DPoseCore::to_ostream(std::ostream &os) const
	{
	  os << "LaserScan6DPose(";
	  getLaser_scan().to_ostream(os);
	  getSensor_pose().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void LaserScan6DPoseCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<laser_scan>";
		getLaser_scan().to_xml(os, indent);
		os << indent << "</laser_scan>";
		os << indent << "<sensor_pose>";
		getSensor_pose().to_xml(os, indent);
		os << indent << "</sensor_pose>";
	}
	
	// restore from xml stream
	void LaserScan6DPoseCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_laser_scan("<laser_scan>");
		static const Smart::KnuthMorrisPratt kmp_sensor_pose("<sensor_pose>");
		
		if(kmp_laser_scan.search(is)) {
			CommBasicObjects::CommLaserScan laser_scanItem;
			laser_scanItem.from_xml(is);
			setLaser_scan(laser_scanItem);
		}
		if(kmp_sensor_pose.search(is)) {
			CommBasicObjects::CommPose3d sensor_poseItem;
			sensor_poseItem.from_xml(is);
			setSensor_pose(sensor_poseItem);
		}
	}
	
	/*
	void LaserScan6DPoseCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommBasicObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommBasicObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see LaserScan6DPoseACE.hh)
		cdr << idl_LaserScan6DPose;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_LaserScan6DPose);
		cdr << data_hash;
		// std::cout << "LaserScan6DPoseCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void LaserScan6DPoseCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommBasicObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see LaserScan6DPoseACE.hh)
		cdr >> idl_LaserScan6DPose;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_LaserScan6DPose);
		assert(data_hash == own_hash);
		// std::cout << "LaserScan6DPoseCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommBasicObjects */
