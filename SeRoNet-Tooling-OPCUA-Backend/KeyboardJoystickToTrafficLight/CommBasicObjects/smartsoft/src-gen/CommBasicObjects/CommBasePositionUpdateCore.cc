//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommBasicObjects/CommBasePositionUpdateCore.hh"

// serialization/deserialization operators
//#include "CommBasicObjects/CommBasePositionUpdateACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommBasicObjects 
{
	const char* CommBasePositionUpdateCore::getCompiledHash()
	{
		return CommBasicObjectsIDL::REPO_HASH;
	}
	
	void CommBasePositionUpdateCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommBasicObjects::CommTimeStamp(idl_CommBasePositionUpdate.timeStamp)
		CommBasicObjects::CommTimeStamp::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommBasePose(idl_CommBasePositionUpdate.pose)
		CommBasicObjects::CommBasePose::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommBasePose(idl_CommBasePositionUpdate.correctedPose)
		CommBasicObjects::CommBasePose::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommBaseVelocity(idl_CommBasePositionUpdate.velocity)
		CommBasicObjects::CommBaseVelocity::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommBaseVelocity(idl_CommBasePositionUpdate.correctedVelocity)
		CommBasicObjects::CommBaseVelocity::getAllHashValues(hashes);
	}
	
	void CommBasePositionUpdateCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommBasePositionUpdateCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommBasicObjects::CommTimeStamp(idl_CommBasePositionUpdate.timeStamp)
		CommBasicObjects::CommTimeStamp::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommBasePose(idl_CommBasePositionUpdate.pose)
		CommBasicObjects::CommBasePose::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommBasePose(idl_CommBasePositionUpdate.correctedPose)
		CommBasicObjects::CommBasePose::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommBaseVelocity(idl_CommBasePositionUpdate.velocity)
		CommBasicObjects::CommBaseVelocity::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommBaseVelocity(idl_CommBasePositionUpdate.correctedVelocity)
		CommBasicObjects::CommBaseVelocity::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommBasePositionUpdateCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += CommBasicObjects::CommTimeStamp::generateDataHash(data.timeStamp);
		seed += CommBasicObjects::CommBasePose::generateDataHash(data.pose);
		seed += CommBasicObjects::CommBasePose::generateDataHash(data.correctedPose);
		seed += CommBasicObjects::CommBaseVelocity::generateDataHash(data.velocity);
		seed += CommBasicObjects::CommBaseVelocity::generateDataHash(data.correctedVelocity);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommBasePositionUpdateCore::CommBasePositionUpdateCore()
	:	idl_CommBasePositionUpdate()
	{  
		setTimeStamp(CommBasicObjects::CommTimeStamp());
		setPose(CommBasicObjects::CommBasePose());
		setCorrectedPose(CommBasicObjects::CommBasePose());
		setVelocity(CommBasicObjects::CommBaseVelocity());
		setCorrectedVelocity(CommBasicObjects::CommBaseVelocity());
	}
	
	CommBasePositionUpdateCore::CommBasePositionUpdateCore(const DATATYPE &data)
	:	idl_CommBasePositionUpdate(data)
	{  }

	CommBasePositionUpdateCore::~CommBasePositionUpdateCore()
	{  }
	
	void CommBasePositionUpdateCore::to_ostream(std::ostream &os) const
	{
	  os << "CommBasePositionUpdate(";
	  getTimeStamp().to_ostream(os);
	  getPose().to_ostream(os);
	  getCorrectedPose().to_ostream(os);
	  getVelocity().to_ostream(os);
	  getCorrectedVelocity().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void CommBasePositionUpdateCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<timeStamp>";
		getTimeStamp().to_xml(os, indent);
		os << indent << "</timeStamp>";
		os << indent << "<pose>";
		getPose().to_xml(os, indent);
		os << indent << "</pose>";
		os << indent << "<correctedPose>";
		getCorrectedPose().to_xml(os, indent);
		os << indent << "</correctedPose>";
		os << indent << "<velocity>";
		getVelocity().to_xml(os, indent);
		os << indent << "</velocity>";
		os << indent << "<correctedVelocity>";
		getCorrectedVelocity().to_xml(os, indent);
		os << indent << "</correctedVelocity>";
	}
	
	// restore from xml stream
	void CommBasePositionUpdateCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_timeStamp("<timeStamp>");
		static const Smart::KnuthMorrisPratt kmp_pose("<pose>");
		static const Smart::KnuthMorrisPratt kmp_correctedPose("<correctedPose>");
		static const Smart::KnuthMorrisPratt kmp_velocity("<velocity>");
		static const Smart::KnuthMorrisPratt kmp_correctedVelocity("<correctedVelocity>");
		
		if(kmp_timeStamp.search(is)) {
			CommBasicObjects::CommTimeStamp timeStampItem;
			timeStampItem.from_xml(is);
			setTimeStamp(timeStampItem);
		}
		if(kmp_pose.search(is)) {
			CommBasicObjects::CommBasePose poseItem;
			poseItem.from_xml(is);
			setPose(poseItem);
		}
		if(kmp_correctedPose.search(is)) {
			CommBasicObjects::CommBasePose correctedPoseItem;
			correctedPoseItem.from_xml(is);
			setCorrectedPose(correctedPoseItem);
		}
		if(kmp_velocity.search(is)) {
			CommBasicObjects::CommBaseVelocity velocityItem;
			velocityItem.from_xml(is);
			setVelocity(velocityItem);
		}
		if(kmp_correctedVelocity.search(is)) {
			CommBasicObjects::CommBaseVelocity correctedVelocityItem;
			correctedVelocityItem.from_xml(is);
			setCorrectedVelocity(correctedVelocityItem);
		}
	}
	
	/*
	void CommBasePositionUpdateCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommBasicObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommBasicObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommBasePositionUpdateACE.hh)
		cdr << idl_CommBasePositionUpdate;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommBasePositionUpdate);
		cdr << data_hash;
		// std::cout << "CommBasePositionUpdateCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommBasePositionUpdateCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommBasicObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommBasePositionUpdateACE.hh)
		cdr >> idl_CommBasePositionUpdate;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommBasePositionUpdate);
		assert(data_hash == own_hash);
		// std::cout << "CommBasePositionUpdateCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommBasicObjects */
