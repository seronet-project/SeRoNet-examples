//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Visualization_msgs_MenuEntryCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Visualization_msgs_MenuEntryACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Visualization_msgs_MenuEntryCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Visualization_msgs_MenuEntryCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
	}
	
	void Visualization_msgs_MenuEntryCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Visualization_msgs_MenuEntryCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
	}
	
	#ifdef ENABLE_HASH
	size_t Visualization_msgs_MenuEntryCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.id);
		boost::hash_combine(seed, data.parent_id);
		boost::hash_combine(seed, std::string(data.title.c_str()));
		boost::hash_combine(seed, std::string(data.command.c_str()));
		boost::hash_combine(seed, data.command_type);
		
		return seed;
	}
	#endif
	
	// default constructor
	Visualization_msgs_MenuEntryCore::Visualization_msgs_MenuEntryCore()
	:	idl_Visualization_msgs_MenuEntry()
	{  
		setId(0);
		setParent_id(0);
		setTitle("");
		setCommand("");
		setCommand_type(0);
	}
	
	Visualization_msgs_MenuEntryCore::Visualization_msgs_MenuEntryCore(const DATATYPE &data)
	:	idl_Visualization_msgs_MenuEntry(data)
	{  }

	Visualization_msgs_MenuEntryCore::~Visualization_msgs_MenuEntryCore()
	{  }
	
	void Visualization_msgs_MenuEntryCore::to_ostream(std::ostream &os) const
	{
	  os << "Visualization_msgs_MenuEntry(";
	  os << getId() << " ";
	  os << getParent_id() << " ";
	  os << getTitle() << " ";
	  os << getCommand() << " ";
	  os << getCommand_type() << " ";
	  os << ") ";
	}
	
	// convert to xml stream
	void Visualization_msgs_MenuEntryCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<id>" << getId() << "</id>";
		os << indent << "<parent_id>" << getParent_id() << "</parent_id>";
		os << indent << "<title>" << getTitle() << "</title>";
		os << indent << "<command>" << getCommand() << "</command>";
		os << indent << "<command_type>" << getCommand_type() << "</command_type>";
	}
	
	// restore from xml stream
	void Visualization_msgs_MenuEntryCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_id("<id>");
		static const Smart::KnuthMorrisPratt kmp_parent_id("<parent_id>");
		static const Smart::KnuthMorrisPratt kmp_title("<title>");
		static const Smart::KnuthMorrisPratt kmp_command("<command>");
		static const Smart::KnuthMorrisPratt kmp_command_type("<command_type>");
		
		if(kmp_id.search(is)) {
			unsigned int idItem;
			is >> idItem;
			setId(idItem);
		}
		if(kmp_parent_id.search(is)) {
			unsigned int parent_idItem;
			is >> parent_idItem;
			setParent_id(parent_idItem);
		}
		if(kmp_title.search(is)) {
			std::string titleItem;
			is >> titleItem;
			setTitle(titleItem);
		}
		if(kmp_command.search(is)) {
			std::string commandItem;
			is >> commandItem;
			setCommand(commandItem);
		}
		if(kmp_command_type.search(is)) {
			unsigned char command_typeItem;
			is >> command_typeItem;
			setCommand_type(command_typeItem);
		}
	}
	
	/*
	void Visualization_msgs_MenuEntryCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Visualization_msgs_MenuEntryACE.hh)
		cdr << idl_Visualization_msgs_MenuEntry;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Visualization_msgs_MenuEntry);
		cdr << data_hash;
		// std::cout << "Visualization_msgs_MenuEntryCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Visualization_msgs_MenuEntryCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Visualization_msgs_MenuEntryACE.hh)
		cdr >> idl_Visualization_msgs_MenuEntry;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Visualization_msgs_MenuEntry);
		assert(data_hash == own_hash);
		// std::cout << "Visualization_msgs_MenuEntryCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
