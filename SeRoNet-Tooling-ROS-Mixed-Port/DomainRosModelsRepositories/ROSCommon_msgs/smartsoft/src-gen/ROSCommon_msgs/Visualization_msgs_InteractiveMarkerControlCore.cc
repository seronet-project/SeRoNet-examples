//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Visualization_msgs_InteractiveMarkerControlCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Visualization_msgs_InteractiveMarkerControlACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Visualization_msgs_InteractiveMarkerControlCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Visualization_msgs_InteractiveMarkerControlCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Quaternion(idl_Visualization_msgs_InteractiveMarkerControl.orientation)
		ROSCommon_msgs::Geometry_msgs_Quaternion::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Visualization_msgs_Marker(idl_Visualization_msgs_InteractiveMarkerControl.markers)
		ROSCommon_msgs::Visualization_msgs_Marker::getAllHashValues(hashes);
	}
	
	void Visualization_msgs_InteractiveMarkerControlCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Visualization_msgs_InteractiveMarkerControlCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Quaternion(idl_Visualization_msgs_InteractiveMarkerControl.orientation)
		ROSCommon_msgs::Geometry_msgs_Quaternion::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Visualization_msgs_Marker(idl_Visualization_msgs_InteractiveMarkerControl.markers)
		ROSCommon_msgs::Visualization_msgs_Marker::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Visualization_msgs_InteractiveMarkerControlCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, std::string(data.name.c_str()));
		seed += ROSCommon_msgs::Geometry_msgs_Quaternion::generateDataHash(data.orientation);
		boost::hash_combine(seed, data.orientation_mode);
		boost::hash_combine(seed, data.interaction_mode);
		boost::hash_combine(seed, data.always_visible);
		std::vector<ROSCommon_msgsIDL::Visualization_msgs_Marker>::const_iterator data_markersIt;
		for(data_markersIt=data.markers.begin(); data_markersIt!=data.markers.end(); data_markersIt++) {
			seed += ROSCommon_msgs::Visualization_msgs_Marker::generateDataHash(*data_markersIt);
		}
		boost::hash_combine(seed, data.independent_marker_orientation);
		boost::hash_combine(seed, std::string(data.description.c_str()));
		
		return seed;
	}
	#endif
	
	// default constructor
	Visualization_msgs_InteractiveMarkerControlCore::Visualization_msgs_InteractiveMarkerControlCore()
	:	idl_Visualization_msgs_InteractiveMarkerControl()
	{  
		setName("");
		setOrientation(ROSCommon_msgs::Geometry_msgs_Quaternion());
		setOrientation_mode(0);
		setInteraction_mode(0);
		setAlways_visible(false);
		setMarkers(std::vector<ROSCommon_msgs::Visualization_msgs_Marker>());
		setIndependent_marker_orientation(false);
		setDescription("");
	}
	
	Visualization_msgs_InteractiveMarkerControlCore::Visualization_msgs_InteractiveMarkerControlCore(const DATATYPE &data)
	:	idl_Visualization_msgs_InteractiveMarkerControl(data)
	{  }

	Visualization_msgs_InteractiveMarkerControlCore::~Visualization_msgs_InteractiveMarkerControlCore()
	{  }
	
	void Visualization_msgs_InteractiveMarkerControlCore::to_ostream(std::ostream &os) const
	{
	  os << "Visualization_msgs_InteractiveMarkerControl(";
	  os << getName() << " ";
	  getOrientation().to_ostream(os);
	  os << getOrientation_mode() << " ";
	  os << getInteraction_mode() << " ";
	  os << getAlways_visible() << " ";
	  std::vector<ROSCommon_msgs::Visualization_msgs_Marker>::const_iterator markersIt;
	  std::vector<ROSCommon_msgs::Visualization_msgs_Marker> markersList = getMarkersCopy();
	  for(markersIt=markersList.begin(); markersIt!=markersList.end(); markersIt++) {
	  	markersIt->to_ostream(os);
	  }
	  os << getIndependent_marker_orientation() << " ";
	  os << getDescription() << " ";
	  os << ") ";
	}
	
	// convert to xml stream
	void Visualization_msgs_InteractiveMarkerControlCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<name>" << getName() << "</name>";
		os << indent << "<orientation>";
		getOrientation().to_xml(os, indent);
		os << indent << "</orientation>";
		os << indent << "<orientation_mode>" << getOrientation_mode() << "</orientation_mode>";
		os << indent << "<interaction_mode>" << getInteraction_mode() << "</interaction_mode>";
		os << indent << "<always_visible>" << getAlways_visible() << "</always_visible>";
		std::vector<ROSCommon_msgs::Visualization_msgs_Marker>::const_iterator markersIt;
		std::vector<ROSCommon_msgs::Visualization_msgs_Marker> markersList = getMarkersCopy();
		counter = 0;
		os << indent << "<markersList n=\"" << markersList.size() << "\">";
		for(markersIt=markersList.begin(); markersIt!=markersList.end(); markersIt++) {
			os << indent << "<markers i=\"" << counter++ << "\">";
			markersIt->to_xml(os, indent);
			os << indent << "</markers>";
		}
		os << indent << "</markersList>";
		os << indent << "<independent_marker_orientation>" << getIndependent_marker_orientation() << "</independent_marker_orientation>";
		os << indent << "<description>" << getDescription() << "</description>";
	}
	
	// restore from xml stream
	void Visualization_msgs_InteractiveMarkerControlCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_name("<name>");
		static const Smart::KnuthMorrisPratt kmp_orientation("<orientation>");
		static const Smart::KnuthMorrisPratt kmp_orientation_mode("<orientation_mode>");
		static const Smart::KnuthMorrisPratt kmp_interaction_mode("<interaction_mode>");
		static const Smart::KnuthMorrisPratt kmp_always_visible("<always_visible>");
		static const Smart::KnuthMorrisPratt kmp_markersList("<markersList n=\"");
		static const Smart::KnuthMorrisPratt kmp_markers("\">");
		static const Smart::KnuthMorrisPratt kmp_independent_marker_orientation("<independent_marker_orientation>");
		static const Smart::KnuthMorrisPratt kmp_description("<description>");
		
		if(kmp_name.search(is)) {
			std::string nameItem;
			is >> nameItem;
			setName(nameItem);
		}
		if(kmp_orientation.search(is)) {
			ROSCommon_msgs::Geometry_msgs_Quaternion orientationItem;
			orientationItem.from_xml(is);
			setOrientation(orientationItem);
		}
		if(kmp_orientation_mode.search(is)) {
			unsigned char orientation_modeItem;
			is >> orientation_modeItem;
			setOrientation_mode(orientation_modeItem);
		}
		if(kmp_interaction_mode.search(is)) {
			unsigned char interaction_modeItem;
			is >> interaction_modeItem;
			setInteraction_mode(interaction_modeItem);
		}
		if(kmp_always_visible.search(is)) {
			bool always_visibleItem;
			is >> always_visibleItem;
			setAlways_visible(always_visibleItem);
		}
		if(kmp_markersList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Visualization_msgs_Marker markersItem;
			std::vector<ROSCommon_msgs::Visualization_msgs_Marker> markersList;
			kmp_markers.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_markers.search(is)) {
					markersItem.from_xml(is);
					markersList.push_back(markersItem);
				}
			}
			setMarkers(markersList);
		}
		if(kmp_independent_marker_orientation.search(is)) {
			bool independent_marker_orientationItem;
			is >> independent_marker_orientationItem;
			setIndependent_marker_orientation(independent_marker_orientationItem);
		}
		if(kmp_description.search(is)) {
			std::string descriptionItem;
			is >> descriptionItem;
			setDescription(descriptionItem);
		}
	}
	
	/*
	void Visualization_msgs_InteractiveMarkerControlCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Visualization_msgs_InteractiveMarkerControlACE.hh)
		cdr << idl_Visualization_msgs_InteractiveMarkerControl;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Visualization_msgs_InteractiveMarkerControl);
		cdr << data_hash;
		// std::cout << "Visualization_msgs_InteractiveMarkerControlCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Visualization_msgs_InteractiveMarkerControlCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Visualization_msgs_InteractiveMarkerControlACE.hh)
		cdr >> idl_Visualization_msgs_InteractiveMarkerControl;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Visualization_msgs_InteractiveMarkerControl);
		assert(data_hash == own_hash);
		// std::cout << "Visualization_msgs_InteractiveMarkerControlCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
