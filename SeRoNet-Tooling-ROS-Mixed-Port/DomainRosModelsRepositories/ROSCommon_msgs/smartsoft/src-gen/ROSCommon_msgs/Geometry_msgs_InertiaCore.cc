//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Geometry_msgs_InertiaCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Geometry_msgs_InertiaACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Geometry_msgs_InertiaCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Geometry_msgs_InertiaCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Vector3(idl_Geometry_msgs_Inertia.com)
		ROSCommon_msgs::Geometry_msgs_Vector3::getAllHashValues(hashes);
	}
	
	void Geometry_msgs_InertiaCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Geometry_msgs_InertiaCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Vector3(idl_Geometry_msgs_Inertia.com)
		ROSCommon_msgs::Geometry_msgs_Vector3::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Geometry_msgs_InertiaCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.m);
		seed += ROSCommon_msgs::Geometry_msgs_Vector3::generateDataHash(data.com);
		boost::hash_combine(seed, data.ixx);
		boost::hash_combine(seed, data.ixy);
		boost::hash_combine(seed, data.ixz);
		boost::hash_combine(seed, data.iyy);
		boost::hash_combine(seed, data.iyz);
		boost::hash_combine(seed, data.izz);
		
		return seed;
	}
	#endif
	
	// default constructor
	Geometry_msgs_InertiaCore::Geometry_msgs_InertiaCore()
	:	idl_Geometry_msgs_Inertia()
	{  
		setM(0.0);
		setCom(ROSCommon_msgs::Geometry_msgs_Vector3());
		setIxx(0.0);
		setIxy(0.0);
		setIxz(0.0);
		setIyy(0.0);
		setIyz(0.0);
		setIzz(0.0);
	}
	
	Geometry_msgs_InertiaCore::Geometry_msgs_InertiaCore(const DATATYPE &data)
	:	idl_Geometry_msgs_Inertia(data)
	{  }

	Geometry_msgs_InertiaCore::~Geometry_msgs_InertiaCore()
	{  }
	
	void Geometry_msgs_InertiaCore::to_ostream(std::ostream &os) const
	{
	  os << "Geometry_msgs_Inertia(";
	  os << getM() << " ";
	  getCom().to_ostream(os);
	  os << getIxx() << " ";
	  os << getIxy() << " ";
	  os << getIxz() << " ";
	  os << getIyy() << " ";
	  os << getIyz() << " ";
	  os << getIzz() << " ";
	  os << ") ";
	}
	
	// convert to xml stream
	void Geometry_msgs_InertiaCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<m>" << getM() << "</m>";
		os << indent << "<com>";
		getCom().to_xml(os, indent);
		os << indent << "</com>";
		os << indent << "<ixx>" << getIxx() << "</ixx>";
		os << indent << "<ixy>" << getIxy() << "</ixy>";
		os << indent << "<ixz>" << getIxz() << "</ixz>";
		os << indent << "<iyy>" << getIyy() << "</iyy>";
		os << indent << "<iyz>" << getIyz() << "</iyz>";
		os << indent << "<izz>" << getIzz() << "</izz>";
	}
	
	// restore from xml stream
	void Geometry_msgs_InertiaCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_m("<m>");
		static const Smart::KnuthMorrisPratt kmp_com("<com>");
		static const Smart::KnuthMorrisPratt kmp_ixx("<ixx>");
		static const Smart::KnuthMorrisPratt kmp_ixy("<ixy>");
		static const Smart::KnuthMorrisPratt kmp_ixz("<ixz>");
		static const Smart::KnuthMorrisPratt kmp_iyy("<iyy>");
		static const Smart::KnuthMorrisPratt kmp_iyz("<iyz>");
		static const Smart::KnuthMorrisPratt kmp_izz("<izz>");
		
		if(kmp_m.search(is)) {
			double mItem;
			is >> mItem;
			setM(mItem);
		}
		if(kmp_com.search(is)) {
			ROSCommon_msgs::Geometry_msgs_Vector3 comItem;
			comItem.from_xml(is);
			setCom(comItem);
		}
		if(kmp_ixx.search(is)) {
			double ixxItem;
			is >> ixxItem;
			setIxx(ixxItem);
		}
		if(kmp_ixy.search(is)) {
			double ixyItem;
			is >> ixyItem;
			setIxy(ixyItem);
		}
		if(kmp_ixz.search(is)) {
			double ixzItem;
			is >> ixzItem;
			setIxz(ixzItem);
		}
		if(kmp_iyy.search(is)) {
			double iyyItem;
			is >> iyyItem;
			setIyy(iyyItem);
		}
		if(kmp_iyz.search(is)) {
			double iyzItem;
			is >> iyzItem;
			setIyz(iyzItem);
		}
		if(kmp_izz.search(is)) {
			double izzItem;
			is >> izzItem;
			setIzz(izzItem);
		}
	}
	
	/*
	void Geometry_msgs_InertiaCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Geometry_msgs_InertiaACE.hh)
		cdr << idl_Geometry_msgs_Inertia;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Geometry_msgs_Inertia);
		cdr << data_hash;
		// std::cout << "Geometry_msgs_InertiaCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Geometry_msgs_InertiaCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Geometry_msgs_InertiaACE.hh)
		cdr >> idl_Geometry_msgs_Inertia;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Geometry_msgs_Inertia);
		assert(data_hash == own_hash);
		// std::cout << "Geometry_msgs_InertiaCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
