//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Nav_msgs_GridCellsCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Nav_msgs_GridCellsACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Nav_msgs_GridCellsCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Nav_msgs_GridCellsCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Nav_msgs_GridCells.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Nav_msgs_GridCells.cells)
		ROSCommon_msgs::Geometry_msgs_Point::getAllHashValues(hashes);
	}
	
	void Nav_msgs_GridCellsCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Nav_msgs_GridCellsCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Nav_msgs_GridCells.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Nav_msgs_GridCells.cells)
		ROSCommon_msgs::Geometry_msgs_Point::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Nav_msgs_GridCellsCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		boost::hash_combine(seed, data.cell_width);
		boost::hash_combine(seed, data.cell_height);
		std::vector<ROSCommon_msgsIDL::Geometry_msgs_Point>::const_iterator data_cellsIt;
		for(data_cellsIt=data.cells.begin(); data_cellsIt!=data.cells.end(); data_cellsIt++) {
			seed += ROSCommon_msgs::Geometry_msgs_Point::generateDataHash(*data_cellsIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Nav_msgs_GridCellsCore::Nav_msgs_GridCellsCore()
	:	idl_Nav_msgs_GridCells()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setCell_width(0.0);
		setCell_height(0.0);
		setCells(std::vector<ROSCommon_msgs::Geometry_msgs_Point>());
	}
	
	Nav_msgs_GridCellsCore::Nav_msgs_GridCellsCore(const DATATYPE &data)
	:	idl_Nav_msgs_GridCells(data)
	{  }

	Nav_msgs_GridCellsCore::~Nav_msgs_GridCellsCore()
	{  }
	
	void Nav_msgs_GridCellsCore::to_ostream(std::ostream &os) const
	{
	  os << "Nav_msgs_GridCells(";
	  getHeader().to_ostream(os);
	  os << getCell_width() << " ";
	  os << getCell_height() << " ";
	  std::vector<ROSCommon_msgs::Geometry_msgs_Point>::const_iterator cellsIt;
	  std::vector<ROSCommon_msgs::Geometry_msgs_Point> cellsList = getCellsCopy();
	  for(cellsIt=cellsList.begin(); cellsIt!=cellsList.end(); cellsIt++) {
	  	cellsIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Nav_msgs_GridCellsCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<cell_width>" << getCell_width() << "</cell_width>";
		os << indent << "<cell_height>" << getCell_height() << "</cell_height>";
		std::vector<ROSCommon_msgs::Geometry_msgs_Point>::const_iterator cellsIt;
		std::vector<ROSCommon_msgs::Geometry_msgs_Point> cellsList = getCellsCopy();
		counter = 0;
		os << indent << "<cellsList n=\"" << cellsList.size() << "\">";
		for(cellsIt=cellsList.begin(); cellsIt!=cellsList.end(); cellsIt++) {
			os << indent << "<cells i=\"" << counter++ << "\">";
			cellsIt->to_xml(os, indent);
			os << indent << "</cells>";
		}
		os << indent << "</cellsList>";
	}
	
	// restore from xml stream
	void Nav_msgs_GridCellsCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_cell_width("<cell_width>");
		static const Smart::KnuthMorrisPratt kmp_cell_height("<cell_height>");
		static const Smart::KnuthMorrisPratt kmp_cellsList("<cellsList n=\"");
		static const Smart::KnuthMorrisPratt kmp_cells("\">");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_cell_width.search(is)) {
			float cell_widthItem;
			is >> cell_widthItem;
			setCell_width(cell_widthItem);
		}
		if(kmp_cell_height.search(is)) {
			float cell_heightItem;
			is >> cell_heightItem;
			setCell_height(cell_heightItem);
		}
		if(kmp_cellsList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Geometry_msgs_Point cellsItem;
			std::vector<ROSCommon_msgs::Geometry_msgs_Point> cellsList;
			kmp_cells.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_cells.search(is)) {
					cellsItem.from_xml(is);
					cellsList.push_back(cellsItem);
				}
			}
			setCells(cellsList);
		}
	}
	
	/*
	void Nav_msgs_GridCellsCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Nav_msgs_GridCellsACE.hh)
		cdr << idl_Nav_msgs_GridCells;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Nav_msgs_GridCells);
		cdr << data_hash;
		// std::cout << "Nav_msgs_GridCellsCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Nav_msgs_GridCellsCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Nav_msgs_GridCellsACE.hh)
		cdr >> idl_Nav_msgs_GridCells;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Nav_msgs_GridCells);
		assert(data_hash == own_hash);
		// std::cout << "Nav_msgs_GridCellsCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
