//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Trajectory_msgs_MultiDOFJointTrajectoryCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Trajectory_msgs_MultiDOFJointTrajectoryACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Trajectory_msgs_MultiDOFJointTrajectoryCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Trajectory_msgs_MultiDOFJointTrajectoryCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Trajectory_msgs_MultiDOFJointTrajectory.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint(idl_Trajectory_msgs_MultiDOFJointTrajectory.points)
		ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint::getAllHashValues(hashes);
	}
	
	void Trajectory_msgs_MultiDOFJointTrajectoryCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Trajectory_msgs_MultiDOFJointTrajectoryCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Trajectory_msgs_MultiDOFJointTrajectory.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint(idl_Trajectory_msgs_MultiDOFJointTrajectory.points)
		ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Trajectory_msgs_MultiDOFJointTrajectoryCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		std::vector<ACE_CString>::const_iterator data_joint_namesIt;
		for(data_joint_namesIt=data.joint_names.begin(); data_joint_namesIt!=data.joint_names.end(); data_joint_namesIt++) {
			boost::hash_combine(seed, std::string(data_joint_namesIt->c_str()));
		}
		std::vector<ROSCommon_msgsIDL::Trajectory_msgs_MultiDOFJointTrajectoryPoint>::const_iterator data_pointsIt;
		for(data_pointsIt=data.points.begin(); data_pointsIt!=data.points.end(); data_pointsIt++) {
			seed += ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint::generateDataHash(*data_pointsIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Trajectory_msgs_MultiDOFJointTrajectoryCore::Trajectory_msgs_MultiDOFJointTrajectoryCore()
	:	idl_Trajectory_msgs_MultiDOFJointTrajectory()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setJoint_names(std::vector<std::string>());
		setPoints(std::vector<ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint>());
	}
	
	Trajectory_msgs_MultiDOFJointTrajectoryCore::Trajectory_msgs_MultiDOFJointTrajectoryCore(const DATATYPE &data)
	:	idl_Trajectory_msgs_MultiDOFJointTrajectory(data)
	{  }

	Trajectory_msgs_MultiDOFJointTrajectoryCore::~Trajectory_msgs_MultiDOFJointTrajectoryCore()
	{  }
	
	void Trajectory_msgs_MultiDOFJointTrajectoryCore::to_ostream(std::ostream &os) const
	{
	  os << "Trajectory_msgs_MultiDOFJointTrajectory(";
	  getHeader().to_ostream(os);
	  std::vector<std::string>::const_iterator joint_namesIt;
	  std::vector<std::string> joint_namesList = getJoint_namesCopy();
	  for(joint_namesIt=joint_namesList.begin(); joint_namesIt!=joint_namesList.end(); joint_namesIt++) {
	  	os << *joint_namesIt << " ";
	  }
	  std::vector<ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint>::const_iterator pointsIt;
	  std::vector<ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint> pointsList = getPointsCopy();
	  for(pointsIt=pointsList.begin(); pointsIt!=pointsList.end(); pointsIt++) {
	  	pointsIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Trajectory_msgs_MultiDOFJointTrajectoryCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		std::vector<std::string>::const_iterator joint_namesIt;
		std::vector<std::string> joint_namesList = getJoint_namesCopy();
		counter = 0;
		os << indent << "<joint_namesList n=\"" << joint_namesList.size() << "\">";
		for(joint_namesIt=joint_namesList.begin(); joint_namesIt!=joint_namesList.end(); joint_namesIt++) {
			os << indent << "<joint_names i=\"" << counter++ << "\">" << *joint_namesIt << "</joint_names>";
		}
		os << indent << "</joint_namesList>";
		std::vector<ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint>::const_iterator pointsIt;
		std::vector<ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint> pointsList = getPointsCopy();
		counter = 0;
		os << indent << "<pointsList n=\"" << pointsList.size() << "\">";
		for(pointsIt=pointsList.begin(); pointsIt!=pointsList.end(); pointsIt++) {
			os << indent << "<points i=\"" << counter++ << "\">";
			pointsIt->to_xml(os, indent);
			os << indent << "</points>";
		}
		os << indent << "</pointsList>";
	}
	
	// restore from xml stream
	void Trajectory_msgs_MultiDOFJointTrajectoryCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_joint_namesList("<joint_namesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_joint_names("\">");
		static const Smart::KnuthMorrisPratt kmp_pointsList("<pointsList n=\"");
		static const Smart::KnuthMorrisPratt kmp_points("\">");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_joint_namesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			std::string joint_namesItem;
			std::vector<std::string> joint_namesList;
			kmp_joint_names.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_joint_names.search(is)) {
					is >> joint_namesItem;
					joint_namesList.push_back(joint_namesItem);
				}
			}
			setJoint_names(joint_namesList);
		}
		if(kmp_pointsList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint pointsItem;
			std::vector<ROSCommon_msgs::Trajectory_msgs_MultiDOFJointTrajectoryPoint> pointsList;
			kmp_points.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_points.search(is)) {
					pointsItem.from_xml(is);
					pointsList.push_back(pointsItem);
				}
			}
			setPoints(pointsList);
		}
	}
	
	/*
	void Trajectory_msgs_MultiDOFJointTrajectoryCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Trajectory_msgs_MultiDOFJointTrajectoryACE.hh)
		cdr << idl_Trajectory_msgs_MultiDOFJointTrajectory;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Trajectory_msgs_MultiDOFJointTrajectory);
		cdr << data_hash;
		// std::cout << "Trajectory_msgs_MultiDOFJointTrajectoryCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Trajectory_msgs_MultiDOFJointTrajectoryCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Trajectory_msgs_MultiDOFJointTrajectoryACE.hh)
		cdr >> idl_Trajectory_msgs_MultiDOFJointTrajectory;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Trajectory_msgs_MultiDOFJointTrajectory);
		assert(data_hash == own_hash);
		// std::cout << "Trajectory_msgs_MultiDOFJointTrajectoryCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
