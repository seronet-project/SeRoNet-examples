//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Shape_msgs_MeshCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Shape_msgs_MeshACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Shape_msgs_MeshCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Shape_msgs_MeshCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSCommon_msgs::Shape_msgs_MeshTriangle(idl_Shape_msgs_Mesh.triangles)
		ROSCommon_msgs::Shape_msgs_MeshTriangle::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Shape_msgs_Mesh.vertices)
		ROSCommon_msgs::Geometry_msgs_Point::getAllHashValues(hashes);
	}
	
	void Shape_msgs_MeshCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Shape_msgs_MeshCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSCommon_msgs::Shape_msgs_MeshTriangle(idl_Shape_msgs_Mesh.triangles)
		ROSCommon_msgs::Shape_msgs_MeshTriangle::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Shape_msgs_Mesh.vertices)
		ROSCommon_msgs::Geometry_msgs_Point::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Shape_msgs_MeshCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		std::vector<ROSCommon_msgsIDL::Shape_msgs_MeshTriangle>::const_iterator data_trianglesIt;
		for(data_trianglesIt=data.triangles.begin(); data_trianglesIt!=data.triangles.end(); data_trianglesIt++) {
			seed += ROSCommon_msgs::Shape_msgs_MeshTriangle::generateDataHash(*data_trianglesIt);
		}
		std::vector<ROSCommon_msgsIDL::Geometry_msgs_Point>::const_iterator data_verticesIt;
		for(data_verticesIt=data.vertices.begin(); data_verticesIt!=data.vertices.end(); data_verticesIt++) {
			seed += ROSCommon_msgs::Geometry_msgs_Point::generateDataHash(*data_verticesIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Shape_msgs_MeshCore::Shape_msgs_MeshCore()
	:	idl_Shape_msgs_Mesh()
	{  
		setTriangles(std::vector<ROSCommon_msgs::Shape_msgs_MeshTriangle>());
		setVertices(std::vector<ROSCommon_msgs::Geometry_msgs_Point>());
	}
	
	Shape_msgs_MeshCore::Shape_msgs_MeshCore(const DATATYPE &data)
	:	idl_Shape_msgs_Mesh(data)
	{  }

	Shape_msgs_MeshCore::~Shape_msgs_MeshCore()
	{  }
	
	void Shape_msgs_MeshCore::to_ostream(std::ostream &os) const
	{
	  os << "Shape_msgs_Mesh(";
	  std::vector<ROSCommon_msgs::Shape_msgs_MeshTriangle>::const_iterator trianglesIt;
	  std::vector<ROSCommon_msgs::Shape_msgs_MeshTriangle> trianglesList = getTrianglesCopy();
	  for(trianglesIt=trianglesList.begin(); trianglesIt!=trianglesList.end(); trianglesIt++) {
	  	trianglesIt->to_ostream(os);
	  }
	  std::vector<ROSCommon_msgs::Geometry_msgs_Point>::const_iterator verticesIt;
	  std::vector<ROSCommon_msgs::Geometry_msgs_Point> verticesList = getVerticesCopy();
	  for(verticesIt=verticesList.begin(); verticesIt!=verticesList.end(); verticesIt++) {
	  	verticesIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Shape_msgs_MeshCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		std::vector<ROSCommon_msgs::Shape_msgs_MeshTriangle>::const_iterator trianglesIt;
		std::vector<ROSCommon_msgs::Shape_msgs_MeshTriangle> trianglesList = getTrianglesCopy();
		counter = 0;
		os << indent << "<trianglesList n=\"" << trianglesList.size() << "\">";
		for(trianglesIt=trianglesList.begin(); trianglesIt!=trianglesList.end(); trianglesIt++) {
			os << indent << "<triangles i=\"" << counter++ << "\">";
			trianglesIt->to_xml(os, indent);
			os << indent << "</triangles>";
		}
		os << indent << "</trianglesList>";
		std::vector<ROSCommon_msgs::Geometry_msgs_Point>::const_iterator verticesIt;
		std::vector<ROSCommon_msgs::Geometry_msgs_Point> verticesList = getVerticesCopy();
		counter = 0;
		os << indent << "<verticesList n=\"" << verticesList.size() << "\">";
		for(verticesIt=verticesList.begin(); verticesIt!=verticesList.end(); verticesIt++) {
			os << indent << "<vertices i=\"" << counter++ << "\">";
			verticesIt->to_xml(os, indent);
			os << indent << "</vertices>";
		}
		os << indent << "</verticesList>";
	}
	
	// restore from xml stream
	void Shape_msgs_MeshCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_trianglesList("<trianglesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_triangles("\">");
		static const Smart::KnuthMorrisPratt kmp_verticesList("<verticesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_vertices("\">");
		
		if(kmp_trianglesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Shape_msgs_MeshTriangle trianglesItem;
			std::vector<ROSCommon_msgs::Shape_msgs_MeshTriangle> trianglesList;
			kmp_triangles.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_triangles.search(is)) {
					trianglesItem.from_xml(is);
					trianglesList.push_back(trianglesItem);
				}
			}
			setTriangles(trianglesList);
		}
		if(kmp_verticesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Geometry_msgs_Point verticesItem;
			std::vector<ROSCommon_msgs::Geometry_msgs_Point> verticesList;
			kmp_vertices.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_vertices.search(is)) {
					verticesItem.from_xml(is);
					verticesList.push_back(verticesItem);
				}
			}
			setVertices(verticesList);
		}
	}
	
	/*
	void Shape_msgs_MeshCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Shape_msgs_MeshACE.hh)
		cdr << idl_Shape_msgs_Mesh;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Shape_msgs_Mesh);
		cdr << data_hash;
		// std::cout << "Shape_msgs_MeshCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Shape_msgs_MeshCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Shape_msgs_MeshACE.hh)
		cdr >> idl_Shape_msgs_Mesh;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Shape_msgs_Mesh);
		assert(data_hash == own_hash);
		// std::cout << "Shape_msgs_MeshCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
