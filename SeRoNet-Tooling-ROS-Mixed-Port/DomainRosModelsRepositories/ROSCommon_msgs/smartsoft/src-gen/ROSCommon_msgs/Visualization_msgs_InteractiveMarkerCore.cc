//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Visualization_msgs_InteractiveMarkerCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Visualization_msgs_InteractiveMarkerACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Visualization_msgs_InteractiveMarkerCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Visualization_msgs_InteractiveMarkerCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Visualization_msgs_InteractiveMarker.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Pose(idl_Visualization_msgs_InteractiveMarker.pose)
		ROSCommon_msgs::Geometry_msgs_Pose::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Visualization_msgs_MenuEntry(idl_Visualization_msgs_InteractiveMarker.menu_entries)
		ROSCommon_msgs::Visualization_msgs_MenuEntry::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl(idl_Visualization_msgs_InteractiveMarker.controls)
		ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl::getAllHashValues(hashes);
	}
	
	void Visualization_msgs_InteractiveMarkerCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Visualization_msgs_InteractiveMarkerCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Visualization_msgs_InteractiveMarker.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Pose(idl_Visualization_msgs_InteractiveMarker.pose)
		ROSCommon_msgs::Geometry_msgs_Pose::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Visualization_msgs_MenuEntry(idl_Visualization_msgs_InteractiveMarker.menu_entries)
		ROSCommon_msgs::Visualization_msgs_MenuEntry::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl(idl_Visualization_msgs_InteractiveMarker.controls)
		ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Visualization_msgs_InteractiveMarkerCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		seed += ROSCommon_msgs::Geometry_msgs_Pose::generateDataHash(data.pose);
		boost::hash_combine(seed, std::string(data.name.c_str()));
		boost::hash_combine(seed, std::string(data.description.c_str()));
		boost::hash_combine(seed, data.scale);
		std::vector<ROSCommon_msgsIDL::Visualization_msgs_MenuEntry>::const_iterator data_menu_entriesIt;
		for(data_menu_entriesIt=data.menu_entries.begin(); data_menu_entriesIt!=data.menu_entries.end(); data_menu_entriesIt++) {
			seed += ROSCommon_msgs::Visualization_msgs_MenuEntry::generateDataHash(*data_menu_entriesIt);
		}
		std::vector<ROSCommon_msgsIDL::Visualization_msgs_InteractiveMarkerControl>::const_iterator data_controlsIt;
		for(data_controlsIt=data.controls.begin(); data_controlsIt!=data.controls.end(); data_controlsIt++) {
			seed += ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl::generateDataHash(*data_controlsIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Visualization_msgs_InteractiveMarkerCore::Visualization_msgs_InteractiveMarkerCore()
	:	idl_Visualization_msgs_InteractiveMarker()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setPose(ROSCommon_msgs::Geometry_msgs_Pose());
		setName("");
		setDescription("");
		setScale(0.0);
		setMenu_entries(std::vector<ROSCommon_msgs::Visualization_msgs_MenuEntry>());
		setControls(std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl>());
	}
	
	Visualization_msgs_InteractiveMarkerCore::Visualization_msgs_InteractiveMarkerCore(const DATATYPE &data)
	:	idl_Visualization_msgs_InteractiveMarker(data)
	{  }

	Visualization_msgs_InteractiveMarkerCore::~Visualization_msgs_InteractiveMarkerCore()
	{  }
	
	void Visualization_msgs_InteractiveMarkerCore::to_ostream(std::ostream &os) const
	{
	  os << "Visualization_msgs_InteractiveMarker(";
	  getHeader().to_ostream(os);
	  getPose().to_ostream(os);
	  os << getName() << " ";
	  os << getDescription() << " ";
	  os << getScale() << " ";
	  std::vector<ROSCommon_msgs::Visualization_msgs_MenuEntry>::const_iterator menu_entriesIt;
	  std::vector<ROSCommon_msgs::Visualization_msgs_MenuEntry> menu_entriesList = getMenu_entriesCopy();
	  for(menu_entriesIt=menu_entriesList.begin(); menu_entriesIt!=menu_entriesList.end(); menu_entriesIt++) {
	  	menu_entriesIt->to_ostream(os);
	  }
	  std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl>::const_iterator controlsIt;
	  std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl> controlsList = getControlsCopy();
	  for(controlsIt=controlsList.begin(); controlsIt!=controlsList.end(); controlsIt++) {
	  	controlsIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Visualization_msgs_InteractiveMarkerCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<pose>";
		getPose().to_xml(os, indent);
		os << indent << "</pose>";
		os << indent << "<name>" << getName() << "</name>";
		os << indent << "<description>" << getDescription() << "</description>";
		os << indent << "<scale>" << getScale() << "</scale>";
		std::vector<ROSCommon_msgs::Visualization_msgs_MenuEntry>::const_iterator menu_entriesIt;
		std::vector<ROSCommon_msgs::Visualization_msgs_MenuEntry> menu_entriesList = getMenu_entriesCopy();
		counter = 0;
		os << indent << "<menu_entriesList n=\"" << menu_entriesList.size() << "\">";
		for(menu_entriesIt=menu_entriesList.begin(); menu_entriesIt!=menu_entriesList.end(); menu_entriesIt++) {
			os << indent << "<menu_entries i=\"" << counter++ << "\">";
			menu_entriesIt->to_xml(os, indent);
			os << indent << "</menu_entries>";
		}
		os << indent << "</menu_entriesList>";
		std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl>::const_iterator controlsIt;
		std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl> controlsList = getControlsCopy();
		counter = 0;
		os << indent << "<controlsList n=\"" << controlsList.size() << "\">";
		for(controlsIt=controlsList.begin(); controlsIt!=controlsList.end(); controlsIt++) {
			os << indent << "<controls i=\"" << counter++ << "\">";
			controlsIt->to_xml(os, indent);
			os << indent << "</controls>";
		}
		os << indent << "</controlsList>";
	}
	
	// restore from xml stream
	void Visualization_msgs_InteractiveMarkerCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_pose("<pose>");
		static const Smart::KnuthMorrisPratt kmp_name("<name>");
		static const Smart::KnuthMorrisPratt kmp_description("<description>");
		static const Smart::KnuthMorrisPratt kmp_scale("<scale>");
		static const Smart::KnuthMorrisPratt kmp_menu_entriesList("<menu_entriesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_menu_entries("\">");
		static const Smart::KnuthMorrisPratt kmp_controlsList("<controlsList n=\"");
		static const Smart::KnuthMorrisPratt kmp_controls("\">");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_pose.search(is)) {
			ROSCommon_msgs::Geometry_msgs_Pose poseItem;
			poseItem.from_xml(is);
			setPose(poseItem);
		}
		if(kmp_name.search(is)) {
			std::string nameItem;
			is >> nameItem;
			setName(nameItem);
		}
		if(kmp_description.search(is)) {
			std::string descriptionItem;
			is >> descriptionItem;
			setDescription(descriptionItem);
		}
		if(kmp_scale.search(is)) {
			float scaleItem;
			is >> scaleItem;
			setScale(scaleItem);
		}
		if(kmp_menu_entriesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Visualization_msgs_MenuEntry menu_entriesItem;
			std::vector<ROSCommon_msgs::Visualization_msgs_MenuEntry> menu_entriesList;
			kmp_menu_entries.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_menu_entries.search(is)) {
					menu_entriesItem.from_xml(is);
					menu_entriesList.push_back(menu_entriesItem);
				}
			}
			setMenu_entries(menu_entriesList);
		}
		if(kmp_controlsList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl controlsItem;
			std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarkerControl> controlsList;
			kmp_controls.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_controls.search(is)) {
					controlsItem.from_xml(is);
					controlsList.push_back(controlsItem);
				}
			}
			setControls(controlsList);
		}
	}
	
	/*
	void Visualization_msgs_InteractiveMarkerCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Visualization_msgs_InteractiveMarkerACE.hh)
		cdr << idl_Visualization_msgs_InteractiveMarker;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Visualization_msgs_InteractiveMarker);
		cdr << data_hash;
		// std::cout << "Visualization_msgs_InteractiveMarkerCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Visualization_msgs_InteractiveMarkerCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Visualization_msgs_InteractiveMarkerACE.hh)
		cdr >> idl_Visualization_msgs_InteractiveMarker;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Visualization_msgs_InteractiveMarker);
		assert(data_hash == own_hash);
		// std::cout << "Visualization_msgs_InteractiveMarkerCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
