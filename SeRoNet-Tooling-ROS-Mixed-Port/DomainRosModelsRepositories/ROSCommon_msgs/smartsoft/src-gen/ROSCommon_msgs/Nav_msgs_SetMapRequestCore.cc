//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Nav_msgs_SetMapRequestCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Nav_msgs_SetMapRequestACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Nav_msgs_SetMapRequestCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Nav_msgs_SetMapRequestCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSCommon_msgs::Nav_msgs_OccupancyGrid(idl_Nav_msgs_SetMapRequest.map)
		ROSCommon_msgs::Nav_msgs_OccupancyGrid::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_PoseWithCovarianceStamped(idl_Nav_msgs_SetMapRequest.initial_pose)
		ROSCommon_msgs::Geometry_msgs_PoseWithCovarianceStamped::getAllHashValues(hashes);
	}
	
	void Nav_msgs_SetMapRequestCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Nav_msgs_SetMapRequestCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSCommon_msgs::Nav_msgs_OccupancyGrid(idl_Nav_msgs_SetMapRequest.map)
		ROSCommon_msgs::Nav_msgs_OccupancyGrid::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_PoseWithCovarianceStamped(idl_Nav_msgs_SetMapRequest.initial_pose)
		ROSCommon_msgs::Geometry_msgs_PoseWithCovarianceStamped::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Nav_msgs_SetMapRequestCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSCommon_msgs::Nav_msgs_OccupancyGrid::generateDataHash(data.map);
		seed += ROSCommon_msgs::Geometry_msgs_PoseWithCovarianceStamped::generateDataHash(data.initial_pose);
		
		return seed;
	}
	#endif
	
	// default constructor
	Nav_msgs_SetMapRequestCore::Nav_msgs_SetMapRequestCore()
	:	idl_Nav_msgs_SetMapRequest()
	{  
		setMap(ROSCommon_msgs::Nav_msgs_OccupancyGrid());
		setInitial_pose(ROSCommon_msgs::Geometry_msgs_PoseWithCovarianceStamped());
	}
	
	Nav_msgs_SetMapRequestCore::Nav_msgs_SetMapRequestCore(const DATATYPE &data)
	:	idl_Nav_msgs_SetMapRequest(data)
	{  }

	Nav_msgs_SetMapRequestCore::~Nav_msgs_SetMapRequestCore()
	{  }
	
	void Nav_msgs_SetMapRequestCore::to_ostream(std::ostream &os) const
	{
	  os << "Nav_msgs_SetMapRequest(";
	  getMap().to_ostream(os);
	  getInitial_pose().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void Nav_msgs_SetMapRequestCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<map>";
		getMap().to_xml(os, indent);
		os << indent << "</map>";
		os << indent << "<initial_pose>";
		getInitial_pose().to_xml(os, indent);
		os << indent << "</initial_pose>";
	}
	
	// restore from xml stream
	void Nav_msgs_SetMapRequestCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_map("<map>");
		static const Smart::KnuthMorrisPratt kmp_initial_pose("<initial_pose>");
		
		if(kmp_map.search(is)) {
			ROSCommon_msgs::Nav_msgs_OccupancyGrid mapItem;
			mapItem.from_xml(is);
			setMap(mapItem);
		}
		if(kmp_initial_pose.search(is)) {
			ROSCommon_msgs::Geometry_msgs_PoseWithCovarianceStamped initial_poseItem;
			initial_poseItem.from_xml(is);
			setInitial_pose(initial_poseItem);
		}
	}
	
	/*
	void Nav_msgs_SetMapRequestCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Nav_msgs_SetMapRequestACE.hh)
		cdr << idl_Nav_msgs_SetMapRequest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Nav_msgs_SetMapRequest);
		cdr << data_hash;
		// std::cout << "Nav_msgs_SetMapRequestCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Nav_msgs_SetMapRequestCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Nav_msgs_SetMapRequestACE.hh)
		cdr >> idl_Nav_msgs_SetMapRequest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Nav_msgs_SetMapRequest);
		assert(data_hash == own_hash);
		// std::cout << "Nav_msgs_SetMapRequestCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
