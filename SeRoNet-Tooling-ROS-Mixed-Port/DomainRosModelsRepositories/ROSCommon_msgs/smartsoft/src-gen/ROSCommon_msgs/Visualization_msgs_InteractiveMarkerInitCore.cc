//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Visualization_msgs_InteractiveMarkerInitCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Visualization_msgs_InteractiveMarkerInitACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Visualization_msgs_InteractiveMarkerInitCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Visualization_msgs_InteractiveMarkerInitCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSCommon_msgs::Visualization_msgs_InteractiveMarker(idl_Visualization_msgs_InteractiveMarkerInit.markers)
		ROSCommon_msgs::Visualization_msgs_InteractiveMarker::getAllHashValues(hashes);
	}
	
	void Visualization_msgs_InteractiveMarkerInitCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Visualization_msgs_InteractiveMarkerInitCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSCommon_msgs::Visualization_msgs_InteractiveMarker(idl_Visualization_msgs_InteractiveMarkerInit.markers)
		ROSCommon_msgs::Visualization_msgs_InteractiveMarker::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Visualization_msgs_InteractiveMarkerInitCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, std::string(data.server_id.c_str()));
		boost::hash_combine(seed, data.seq_num);
		std::vector<ROSCommon_msgsIDL::Visualization_msgs_InteractiveMarker>::const_iterator data_markersIt;
		for(data_markersIt=data.markers.begin(); data_markersIt!=data.markers.end(); data_markersIt++) {
			seed += ROSCommon_msgs::Visualization_msgs_InteractiveMarker::generateDataHash(*data_markersIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Visualization_msgs_InteractiveMarkerInitCore::Visualization_msgs_InteractiveMarkerInitCore()
	:	idl_Visualization_msgs_InteractiveMarkerInit()
	{  
		setServer_id("");
		setSeq_num(0);
		setMarkers(std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarker>());
	}
	
	Visualization_msgs_InteractiveMarkerInitCore::Visualization_msgs_InteractiveMarkerInitCore(const DATATYPE &data)
	:	idl_Visualization_msgs_InteractiveMarkerInit(data)
	{  }

	Visualization_msgs_InteractiveMarkerInitCore::~Visualization_msgs_InteractiveMarkerInitCore()
	{  }
	
	void Visualization_msgs_InteractiveMarkerInitCore::to_ostream(std::ostream &os) const
	{
	  os << "Visualization_msgs_InteractiveMarkerInit(";
	  os << getServer_id() << " ";
	  os << getSeq_num() << " ";
	  std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarker>::const_iterator markersIt;
	  std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarker> markersList = getMarkersCopy();
	  for(markersIt=markersList.begin(); markersIt!=markersList.end(); markersIt++) {
	  	markersIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Visualization_msgs_InteractiveMarkerInitCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<server_id>" << getServer_id() << "</server_id>";
		os << indent << "<seq_num>" << getSeq_num() << "</seq_num>";
		std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarker>::const_iterator markersIt;
		std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarker> markersList = getMarkersCopy();
		counter = 0;
		os << indent << "<markersList n=\"" << markersList.size() << "\">";
		for(markersIt=markersList.begin(); markersIt!=markersList.end(); markersIt++) {
			os << indent << "<markers i=\"" << counter++ << "\">";
			markersIt->to_xml(os, indent);
			os << indent << "</markers>";
		}
		os << indent << "</markersList>";
	}
	
	// restore from xml stream
	void Visualization_msgs_InteractiveMarkerInitCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_server_id("<server_id>");
		static const Smart::KnuthMorrisPratt kmp_seq_num("<seq_num>");
		static const Smart::KnuthMorrisPratt kmp_markersList("<markersList n=\"");
		static const Smart::KnuthMorrisPratt kmp_markers("\">");
		
		if(kmp_server_id.search(is)) {
			std::string server_idItem;
			is >> server_idItem;
			setServer_id(server_idItem);
		}
		if(kmp_seq_num.search(is)) {
			unsigned long int seq_numItem;
			is >> seq_numItem;
			setSeq_num(seq_numItem);
		}
		if(kmp_markersList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Visualization_msgs_InteractiveMarker markersItem;
			std::vector<ROSCommon_msgs::Visualization_msgs_InteractiveMarker> markersList;
			kmp_markers.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_markers.search(is)) {
					markersItem.from_xml(is);
					markersList.push_back(markersItem);
				}
			}
			setMarkers(markersList);
		}
	}
	
	/*
	void Visualization_msgs_InteractiveMarkerInitCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Visualization_msgs_InteractiveMarkerInitACE.hh)
		cdr << idl_Visualization_msgs_InteractiveMarkerInit;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Visualization_msgs_InteractiveMarkerInit);
		cdr << data_hash;
		// std::cout << "Visualization_msgs_InteractiveMarkerInitCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Visualization_msgs_InteractiveMarkerInitCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Visualization_msgs_InteractiveMarkerInitACE.hh)
		cdr >> idl_Visualization_msgs_InteractiveMarkerInit;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Visualization_msgs_InteractiveMarkerInit);
		assert(data_hash == own_hash);
		// std::cout << "Visualization_msgs_InteractiveMarkerInitCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
