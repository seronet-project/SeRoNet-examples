//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Nav_msgs_OdometryCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Nav_msgs_OdometryACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Nav_msgs_OdometryCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Nav_msgs_OdometryCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Nav_msgs_Odometry.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_PoseWithCovariance(idl_Nav_msgs_Odometry.pose)
		ROSCommon_msgs::Geometry_msgs_PoseWithCovariance::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_TwistWithCovariance(idl_Nav_msgs_Odometry.twist)
		ROSCommon_msgs::Geometry_msgs_TwistWithCovariance::getAllHashValues(hashes);
	}
	
	void Nav_msgs_OdometryCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Nav_msgs_OdometryCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Nav_msgs_Odometry.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_PoseWithCovariance(idl_Nav_msgs_Odometry.pose)
		ROSCommon_msgs::Geometry_msgs_PoseWithCovariance::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_TwistWithCovariance(idl_Nav_msgs_Odometry.twist)
		ROSCommon_msgs::Geometry_msgs_TwistWithCovariance::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Nav_msgs_OdometryCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		boost::hash_combine(seed, std::string(data.child_frame_id.c_str()));
		seed += ROSCommon_msgs::Geometry_msgs_PoseWithCovariance::generateDataHash(data.pose);
		seed += ROSCommon_msgs::Geometry_msgs_TwistWithCovariance::generateDataHash(data.twist);
		
		return seed;
	}
	#endif
	
	// default constructor
	Nav_msgs_OdometryCore::Nav_msgs_OdometryCore()
	:	idl_Nav_msgs_Odometry()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setChild_frame_id("");
		setPose(ROSCommon_msgs::Geometry_msgs_PoseWithCovariance());
		setTwist(ROSCommon_msgs::Geometry_msgs_TwistWithCovariance());
	}
	
	Nav_msgs_OdometryCore::Nav_msgs_OdometryCore(const DATATYPE &data)
	:	idl_Nav_msgs_Odometry(data)
	{  }

	Nav_msgs_OdometryCore::~Nav_msgs_OdometryCore()
	{  }
	
	void Nav_msgs_OdometryCore::to_ostream(std::ostream &os) const
	{
	  os << "Nav_msgs_Odometry(";
	  getHeader().to_ostream(os);
	  os << getChild_frame_id() << " ";
	  getPose().to_ostream(os);
	  getTwist().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void Nav_msgs_OdometryCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<child_frame_id>" << getChild_frame_id() << "</child_frame_id>";
		os << indent << "<pose>";
		getPose().to_xml(os, indent);
		os << indent << "</pose>";
		os << indent << "<twist>";
		getTwist().to_xml(os, indent);
		os << indent << "</twist>";
	}
	
	// restore from xml stream
	void Nav_msgs_OdometryCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_child_frame_id("<child_frame_id>");
		static const Smart::KnuthMorrisPratt kmp_pose("<pose>");
		static const Smart::KnuthMorrisPratt kmp_twist("<twist>");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_child_frame_id.search(is)) {
			std::string child_frame_idItem;
			is >> child_frame_idItem;
			setChild_frame_id(child_frame_idItem);
		}
		if(kmp_pose.search(is)) {
			ROSCommon_msgs::Geometry_msgs_PoseWithCovariance poseItem;
			poseItem.from_xml(is);
			setPose(poseItem);
		}
		if(kmp_twist.search(is)) {
			ROSCommon_msgs::Geometry_msgs_TwistWithCovariance twistItem;
			twistItem.from_xml(is);
			setTwist(twistItem);
		}
	}
	
	/*
	void Nav_msgs_OdometryCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Nav_msgs_OdometryACE.hh)
		cdr << idl_Nav_msgs_Odometry;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Nav_msgs_Odometry);
		cdr << data_hash;
		// std::cout << "Nav_msgs_OdometryCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Nav_msgs_OdometryCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Nav_msgs_OdometryACE.hh)
		cdr >> idl_Nav_msgs_Odometry;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Nav_msgs_Odometry);
		assert(data_hash == own_hash);
		// std::cout << "Nav_msgs_OdometryCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
