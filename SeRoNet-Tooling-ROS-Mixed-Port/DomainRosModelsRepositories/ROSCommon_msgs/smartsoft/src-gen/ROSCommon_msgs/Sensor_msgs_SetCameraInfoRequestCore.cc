//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Sensor_msgs_SetCameraInfoRequestCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Sensor_msgs_SetCameraInfoRequestACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Sensor_msgs_SetCameraInfoRequestCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Sensor_msgs_SetCameraInfoRequestCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSCommon_msgs::Sensor_msgs_CameraInfo(idl_Sensor_msgs_SetCameraInfoRequest.camera_info)
		ROSCommon_msgs::Sensor_msgs_CameraInfo::getAllHashValues(hashes);
	}
	
	void Sensor_msgs_SetCameraInfoRequestCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Sensor_msgs_SetCameraInfoRequestCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSCommon_msgs::Sensor_msgs_CameraInfo(idl_Sensor_msgs_SetCameraInfoRequest.camera_info)
		ROSCommon_msgs::Sensor_msgs_CameraInfo::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Sensor_msgs_SetCameraInfoRequestCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSCommon_msgs::Sensor_msgs_CameraInfo::generateDataHash(data.camera_info);
		
		return seed;
	}
	#endif
	
	// default constructor
	Sensor_msgs_SetCameraInfoRequestCore::Sensor_msgs_SetCameraInfoRequestCore()
	:	idl_Sensor_msgs_SetCameraInfoRequest()
	{  
		setCamera_info(ROSCommon_msgs::Sensor_msgs_CameraInfo());
	}
	
	Sensor_msgs_SetCameraInfoRequestCore::Sensor_msgs_SetCameraInfoRequestCore(const DATATYPE &data)
	:	idl_Sensor_msgs_SetCameraInfoRequest(data)
	{  }

	Sensor_msgs_SetCameraInfoRequestCore::~Sensor_msgs_SetCameraInfoRequestCore()
	{  }
	
	void Sensor_msgs_SetCameraInfoRequestCore::to_ostream(std::ostream &os) const
	{
	  os << "Sensor_msgs_SetCameraInfoRequest(";
	  getCamera_info().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void Sensor_msgs_SetCameraInfoRequestCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<camera_info>";
		getCamera_info().to_xml(os, indent);
		os << indent << "</camera_info>";
	}
	
	// restore from xml stream
	void Sensor_msgs_SetCameraInfoRequestCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_camera_info("<camera_info>");
		
		if(kmp_camera_info.search(is)) {
			ROSCommon_msgs::Sensor_msgs_CameraInfo camera_infoItem;
			camera_infoItem.from_xml(is);
			setCamera_info(camera_infoItem);
		}
	}
	
	/*
	void Sensor_msgs_SetCameraInfoRequestCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Sensor_msgs_SetCameraInfoRequestACE.hh)
		cdr << idl_Sensor_msgs_SetCameraInfoRequest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Sensor_msgs_SetCameraInfoRequest);
		cdr << data_hash;
		// std::cout << "Sensor_msgs_SetCameraInfoRequestCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Sensor_msgs_SetCameraInfoRequestCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Sensor_msgs_SetCameraInfoRequestACE.hh)
		cdr >> idl_Sensor_msgs_SetCameraInfoRequest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Sensor_msgs_SetCameraInfoRequest);
		assert(data_hash == own_hash);
		// std::cout << "Sensor_msgs_SetCameraInfoRequestCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
