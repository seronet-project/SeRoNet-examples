//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Diagnostic_msgs_DiagnosticStatusCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Diagnostic_msgs_DiagnosticStatusACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Diagnostic_msgs_DiagnosticStatusCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Diagnostic_msgs_DiagnosticStatusCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSCommon_msgs::Diagnostic_msgs_KeyValue(idl_Diagnostic_msgs_DiagnosticStatus.values)
		ROSCommon_msgs::Diagnostic_msgs_KeyValue::getAllHashValues(hashes);
	}
	
	void Diagnostic_msgs_DiagnosticStatusCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Diagnostic_msgs_DiagnosticStatusCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSCommon_msgs::Diagnostic_msgs_KeyValue(idl_Diagnostic_msgs_DiagnosticStatus.values)
		ROSCommon_msgs::Diagnostic_msgs_KeyValue::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Diagnostic_msgs_DiagnosticStatusCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.level);
		boost::hash_combine(seed, std::string(data.name.c_str()));
		boost::hash_combine(seed, std::string(data.message.c_str()));
		boost::hash_combine(seed, std::string(data.hardware_id.c_str()));
		std::vector<ROSCommon_msgsIDL::Diagnostic_msgs_KeyValue>::const_iterator data_valuesIt;
		for(data_valuesIt=data.values.begin(); data_valuesIt!=data.values.end(); data_valuesIt++) {
			seed += ROSCommon_msgs::Diagnostic_msgs_KeyValue::generateDataHash(*data_valuesIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Diagnostic_msgs_DiagnosticStatusCore::Diagnostic_msgs_DiagnosticStatusCore()
	:	idl_Diagnostic_msgs_DiagnosticStatus()
	{  
		setLevel(0);
		setName("");
		setMessage("");
		setHardware_id("");
		setValues(std::vector<ROSCommon_msgs::Diagnostic_msgs_KeyValue>());
	}
	
	Diagnostic_msgs_DiagnosticStatusCore::Diagnostic_msgs_DiagnosticStatusCore(const DATATYPE &data)
	:	idl_Diagnostic_msgs_DiagnosticStatus(data)
	{  }

	Diagnostic_msgs_DiagnosticStatusCore::~Diagnostic_msgs_DiagnosticStatusCore()
	{  }
	
	void Diagnostic_msgs_DiagnosticStatusCore::to_ostream(std::ostream &os) const
	{
	  os << "Diagnostic_msgs_DiagnosticStatus(";
	  os << getLevel() << " ";
	  os << getName() << " ";
	  os << getMessage() << " ";
	  os << getHardware_id() << " ";
	  std::vector<ROSCommon_msgs::Diagnostic_msgs_KeyValue>::const_iterator valuesIt;
	  std::vector<ROSCommon_msgs::Diagnostic_msgs_KeyValue> valuesList = getValuesCopy();
	  for(valuesIt=valuesList.begin(); valuesIt!=valuesList.end(); valuesIt++) {
	  	valuesIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Diagnostic_msgs_DiagnosticStatusCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<level>" << getLevel() << "</level>";
		os << indent << "<name>" << getName() << "</name>";
		os << indent << "<message>" << getMessage() << "</message>";
		os << indent << "<hardware_id>" << getHardware_id() << "</hardware_id>";
		std::vector<ROSCommon_msgs::Diagnostic_msgs_KeyValue>::const_iterator valuesIt;
		std::vector<ROSCommon_msgs::Diagnostic_msgs_KeyValue> valuesList = getValuesCopy();
		counter = 0;
		os << indent << "<valuesList n=\"" << valuesList.size() << "\">";
		for(valuesIt=valuesList.begin(); valuesIt!=valuesList.end(); valuesIt++) {
			os << indent << "<values i=\"" << counter++ << "\">";
			valuesIt->to_xml(os, indent);
			os << indent << "</values>";
		}
		os << indent << "</valuesList>";
	}
	
	// restore from xml stream
	void Diagnostic_msgs_DiagnosticStatusCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_level("<level>");
		static const Smart::KnuthMorrisPratt kmp_name("<name>");
		static const Smart::KnuthMorrisPratt kmp_message("<message>");
		static const Smart::KnuthMorrisPratt kmp_hardware_id("<hardware_id>");
		static const Smart::KnuthMorrisPratt kmp_valuesList("<valuesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_values("\">");
		
		if(kmp_level.search(is)) {
			unsigned char levelItem;
			is >> levelItem;
			setLevel(levelItem);
		}
		if(kmp_name.search(is)) {
			std::string nameItem;
			is >> nameItem;
			setName(nameItem);
		}
		if(kmp_message.search(is)) {
			std::string messageItem;
			is >> messageItem;
			setMessage(messageItem);
		}
		if(kmp_hardware_id.search(is)) {
			std::string hardware_idItem;
			is >> hardware_idItem;
			setHardware_id(hardware_idItem);
		}
		if(kmp_valuesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Diagnostic_msgs_KeyValue valuesItem;
			std::vector<ROSCommon_msgs::Diagnostic_msgs_KeyValue> valuesList;
			kmp_values.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_values.search(is)) {
					valuesItem.from_xml(is);
					valuesList.push_back(valuesItem);
				}
			}
			setValues(valuesList);
		}
	}
	
	/*
	void Diagnostic_msgs_DiagnosticStatusCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Diagnostic_msgs_DiagnosticStatusACE.hh)
		cdr << idl_Diagnostic_msgs_DiagnosticStatus;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Diagnostic_msgs_DiagnosticStatus);
		cdr << data_hash;
		// std::cout << "Diagnostic_msgs_DiagnosticStatusCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Diagnostic_msgs_DiagnosticStatusCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Diagnostic_msgs_DiagnosticStatusACE.hh)
		cdr >> idl_Diagnostic_msgs_DiagnosticStatus;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Diagnostic_msgs_DiagnosticStatus);
		assert(data_hash == own_hash);
		// std::cout << "Diagnostic_msgs_DiagnosticStatusCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
