//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Sensor_msgs_RangeCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Sensor_msgs_RangeACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Sensor_msgs_RangeCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Sensor_msgs_RangeCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Sensor_msgs_Range.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
	}
	
	void Sensor_msgs_RangeCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Sensor_msgs_RangeCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Sensor_msgs_Range.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Sensor_msgs_RangeCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		boost::hash_combine(seed, data.radiation_type);
		boost::hash_combine(seed, data.field_of_view);
		boost::hash_combine(seed, data.min_range);
		boost::hash_combine(seed, data.max_range);
		boost::hash_combine(seed, data.range);
		
		return seed;
	}
	#endif
	
	// default constructor
	Sensor_msgs_RangeCore::Sensor_msgs_RangeCore()
	:	idl_Sensor_msgs_Range()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setRadiation_type(0);
		setField_of_view(0.0);
		setMin_range(0.0);
		setMax_range(0.0);
		setRange(0.0);
	}
	
	Sensor_msgs_RangeCore::Sensor_msgs_RangeCore(const DATATYPE &data)
	:	idl_Sensor_msgs_Range(data)
	{  }

	Sensor_msgs_RangeCore::~Sensor_msgs_RangeCore()
	{  }
	
	void Sensor_msgs_RangeCore::to_ostream(std::ostream &os) const
	{
	  os << "Sensor_msgs_Range(";
	  getHeader().to_ostream(os);
	  os << getRadiation_type() << " ";
	  os << getField_of_view() << " ";
	  os << getMin_range() << " ";
	  os << getMax_range() << " ";
	  os << getRange() << " ";
	  os << ") ";
	}
	
	// convert to xml stream
	void Sensor_msgs_RangeCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<radiation_type>" << getRadiation_type() << "</radiation_type>";
		os << indent << "<field_of_view>" << getField_of_view() << "</field_of_view>";
		os << indent << "<min_range>" << getMin_range() << "</min_range>";
		os << indent << "<max_range>" << getMax_range() << "</max_range>";
		os << indent << "<range>" << getRange() << "</range>";
	}
	
	// restore from xml stream
	void Sensor_msgs_RangeCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_radiation_type("<radiation_type>");
		static const Smart::KnuthMorrisPratt kmp_field_of_view("<field_of_view>");
		static const Smart::KnuthMorrisPratt kmp_min_range("<min_range>");
		static const Smart::KnuthMorrisPratt kmp_max_range("<max_range>");
		static const Smart::KnuthMorrisPratt kmp_range("<range>");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_radiation_type.search(is)) {
			unsigned char radiation_typeItem;
			is >> radiation_typeItem;
			setRadiation_type(radiation_typeItem);
		}
		if(kmp_field_of_view.search(is)) {
			float field_of_viewItem;
			is >> field_of_viewItem;
			setField_of_view(field_of_viewItem);
		}
		if(kmp_min_range.search(is)) {
			float min_rangeItem;
			is >> min_rangeItem;
			setMin_range(min_rangeItem);
		}
		if(kmp_max_range.search(is)) {
			float max_rangeItem;
			is >> max_rangeItem;
			setMax_range(max_rangeItem);
		}
		if(kmp_range.search(is)) {
			float rangeItem;
			is >> rangeItem;
			setRange(rangeItem);
		}
	}
	
	/*
	void Sensor_msgs_RangeCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Sensor_msgs_RangeACE.hh)
		cdr << idl_Sensor_msgs_Range;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Sensor_msgs_Range);
		cdr << data_hash;
		// std::cout << "Sensor_msgs_RangeCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Sensor_msgs_RangeCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Sensor_msgs_RangeACE.hh)
		cdr >> idl_Sensor_msgs_Range;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Sensor_msgs_Range);
		assert(data_hash == own_hash);
		// std::cout << "Sensor_msgs_RangeCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
