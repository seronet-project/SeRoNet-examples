//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Sensor_msgs_NavSatFixCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Sensor_msgs_NavSatFixACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Sensor_msgs_NavSatFixCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Sensor_msgs_NavSatFixCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Sensor_msgs_NavSatFix.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Sensor_msgs_NavSatStatus(idl_Sensor_msgs_NavSatFix.status)
		ROSCommon_msgs::Sensor_msgs_NavSatStatus::getAllHashValues(hashes);
	}
	
	void Sensor_msgs_NavSatFixCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Sensor_msgs_NavSatFixCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Sensor_msgs_NavSatFix.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Sensor_msgs_NavSatStatus(idl_Sensor_msgs_NavSatFix.status)
		ROSCommon_msgs::Sensor_msgs_NavSatStatus::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Sensor_msgs_NavSatFixCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		seed += ROSCommon_msgs::Sensor_msgs_NavSatStatus::generateDataHash(data.status);
		boost::hash_combine(seed, data.latitude);
		boost::hash_combine(seed, data.longitude);
		boost::hash_combine(seed, data.altitude);
		std::vector<ACE_CDR::Double>::const_iterator data_position_covarianceIt;
		for(data_position_covarianceIt=data.position_covariance.begin(); data_position_covarianceIt!=data.position_covariance.end(); data_position_covarianceIt++) {
			boost::hash_combine(seed, *data_position_covarianceIt);
		}
		boost::hash_combine(seed, data.position_covariance_type);
		
		return seed;
	}
	#endif
	
	// default constructor
	Sensor_msgs_NavSatFixCore::Sensor_msgs_NavSatFixCore()
	:	idl_Sensor_msgs_NavSatFix()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setStatus(ROSCommon_msgs::Sensor_msgs_NavSatStatus());
		setLatitude(0.0);
		setLongitude(0.0);
		setAltitude(0.0);
		setPosition_covariance(std::vector<double>());
		setPosition_covariance_type(0);
	}
	
	Sensor_msgs_NavSatFixCore::Sensor_msgs_NavSatFixCore(const DATATYPE &data)
	:	idl_Sensor_msgs_NavSatFix(data)
	{  }

	Sensor_msgs_NavSatFixCore::~Sensor_msgs_NavSatFixCore()
	{  }
	
	void Sensor_msgs_NavSatFixCore::to_ostream(std::ostream &os) const
	{
	  os << "Sensor_msgs_NavSatFix(";
	  getHeader().to_ostream(os);
	  getStatus().to_ostream(os);
	  os << getLatitude() << " ";
	  os << getLongitude() << " ";
	  os << getAltitude() << " ";
	  std::vector<double>::const_iterator position_covarianceIt;
	  std::vector<double> position_covarianceList = getPosition_covarianceCopy();
	  for(position_covarianceIt=position_covarianceList.begin(); position_covarianceIt!=position_covarianceList.end(); position_covarianceIt++) {
	  	os << *position_covarianceIt << " ";
	  }
	  os << getPosition_covariance_type() << " ";
	  os << ") ";
	}
	
	// convert to xml stream
	void Sensor_msgs_NavSatFixCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<status>";
		getStatus().to_xml(os, indent);
		os << indent << "</status>";
		os << indent << "<latitude>" << getLatitude() << "</latitude>";
		os << indent << "<longitude>" << getLongitude() << "</longitude>";
		os << indent << "<altitude>" << getAltitude() << "</altitude>";
		std::vector<double>::const_iterator position_covarianceIt;
		std::vector<double> position_covarianceList = getPosition_covarianceCopy();
		counter = 0;
		os << indent << "<position_covarianceList n=\"" << position_covarianceList.size() << "\">";
		for(position_covarianceIt=position_covarianceList.begin(); position_covarianceIt!=position_covarianceList.end(); position_covarianceIt++) {
			os << indent << "<position_covariance i=\"" << counter++ << "\">" << *position_covarianceIt << "</position_covariance>";
		}
		os << indent << "</position_covarianceList>";
		os << indent << "<position_covariance_type>" << getPosition_covariance_type() << "</position_covariance_type>";
	}
	
	// restore from xml stream
	void Sensor_msgs_NavSatFixCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_status("<status>");
		static const Smart::KnuthMorrisPratt kmp_latitude("<latitude>");
		static const Smart::KnuthMorrisPratt kmp_longitude("<longitude>");
		static const Smart::KnuthMorrisPratt kmp_altitude("<altitude>");
		static const Smart::KnuthMorrisPratt kmp_position_covarianceList("<position_covarianceList n=\"");
		static const Smart::KnuthMorrisPratt kmp_position_covariance("\">");
		static const Smart::KnuthMorrisPratt kmp_position_covariance_type("<position_covariance_type>");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_status.search(is)) {
			ROSCommon_msgs::Sensor_msgs_NavSatStatus statusItem;
			statusItem.from_xml(is);
			setStatus(statusItem);
		}
		if(kmp_latitude.search(is)) {
			double latitudeItem;
			is >> latitudeItem;
			setLatitude(latitudeItem);
		}
		if(kmp_longitude.search(is)) {
			double longitudeItem;
			is >> longitudeItem;
			setLongitude(longitudeItem);
		}
		if(kmp_altitude.search(is)) {
			double altitudeItem;
			is >> altitudeItem;
			setAltitude(altitudeItem);
		}
		if(kmp_position_covarianceList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			double position_covarianceItem;
			std::vector<double> position_covarianceList;
			kmp_position_covariance.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_position_covariance.search(is)) {
					is >> position_covarianceItem;
					position_covarianceList.push_back(position_covarianceItem);
				}
			}
			setPosition_covariance(position_covarianceList);
		}
		if(kmp_position_covariance_type.search(is)) {
			unsigned char position_covariance_typeItem;
			is >> position_covariance_typeItem;
			setPosition_covariance_type(position_covariance_typeItem);
		}
	}
	
	/*
	void Sensor_msgs_NavSatFixCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Sensor_msgs_NavSatFixACE.hh)
		cdr << idl_Sensor_msgs_NavSatFix;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Sensor_msgs_NavSatFix);
		cdr << data_hash;
		// std::cout << "Sensor_msgs_NavSatFixCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Sensor_msgs_NavSatFixCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Sensor_msgs_NavSatFixACE.hh)
		cdr >> idl_Sensor_msgs_NavSatFix;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Sensor_msgs_NavSatFix);
		assert(data_hash == own_hash);
		// std::cout << "Sensor_msgs_NavSatFixCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
