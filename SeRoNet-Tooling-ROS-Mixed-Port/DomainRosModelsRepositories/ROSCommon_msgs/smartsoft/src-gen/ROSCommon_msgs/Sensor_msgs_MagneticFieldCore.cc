//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Sensor_msgs_MagneticFieldCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Sensor_msgs_MagneticFieldACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Sensor_msgs_MagneticFieldCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Sensor_msgs_MagneticFieldCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Sensor_msgs_MagneticField.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Vector3(idl_Sensor_msgs_MagneticField.magnetic_field)
		ROSCommon_msgs::Geometry_msgs_Vector3::getAllHashValues(hashes);
	}
	
	void Sensor_msgs_MagneticFieldCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Sensor_msgs_MagneticFieldCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Sensor_msgs_MagneticField.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Vector3(idl_Sensor_msgs_MagneticField.magnetic_field)
		ROSCommon_msgs::Geometry_msgs_Vector3::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Sensor_msgs_MagneticFieldCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		seed += ROSCommon_msgs::Geometry_msgs_Vector3::generateDataHash(data.magnetic_field);
		std::vector<ACE_CDR::Double>::const_iterator data_magnetic_field_covarianceIt;
		for(data_magnetic_field_covarianceIt=data.magnetic_field_covariance.begin(); data_magnetic_field_covarianceIt!=data.magnetic_field_covariance.end(); data_magnetic_field_covarianceIt++) {
			boost::hash_combine(seed, *data_magnetic_field_covarianceIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Sensor_msgs_MagneticFieldCore::Sensor_msgs_MagneticFieldCore()
	:	idl_Sensor_msgs_MagneticField()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setMagnetic_field(ROSCommon_msgs::Geometry_msgs_Vector3());
		setMagnetic_field_covariance(std::vector<double>());
	}
	
	Sensor_msgs_MagneticFieldCore::Sensor_msgs_MagneticFieldCore(const DATATYPE &data)
	:	idl_Sensor_msgs_MagneticField(data)
	{  }

	Sensor_msgs_MagneticFieldCore::~Sensor_msgs_MagneticFieldCore()
	{  }
	
	void Sensor_msgs_MagneticFieldCore::to_ostream(std::ostream &os) const
	{
	  os << "Sensor_msgs_MagneticField(";
	  getHeader().to_ostream(os);
	  getMagnetic_field().to_ostream(os);
	  std::vector<double>::const_iterator magnetic_field_covarianceIt;
	  std::vector<double> magnetic_field_covarianceList = getMagnetic_field_covarianceCopy();
	  for(magnetic_field_covarianceIt=magnetic_field_covarianceList.begin(); magnetic_field_covarianceIt!=magnetic_field_covarianceList.end(); magnetic_field_covarianceIt++) {
	  	os << *magnetic_field_covarianceIt << " ";
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Sensor_msgs_MagneticFieldCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<magnetic_field>";
		getMagnetic_field().to_xml(os, indent);
		os << indent << "</magnetic_field>";
		std::vector<double>::const_iterator magnetic_field_covarianceIt;
		std::vector<double> magnetic_field_covarianceList = getMagnetic_field_covarianceCopy();
		counter = 0;
		os << indent << "<magnetic_field_covarianceList n=\"" << magnetic_field_covarianceList.size() << "\">";
		for(magnetic_field_covarianceIt=magnetic_field_covarianceList.begin(); magnetic_field_covarianceIt!=magnetic_field_covarianceList.end(); magnetic_field_covarianceIt++) {
			os << indent << "<magnetic_field_covariance i=\"" << counter++ << "\">" << *magnetic_field_covarianceIt << "</magnetic_field_covariance>";
		}
		os << indent << "</magnetic_field_covarianceList>";
	}
	
	// restore from xml stream
	void Sensor_msgs_MagneticFieldCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_magnetic_field("<magnetic_field>");
		static const Smart::KnuthMorrisPratt kmp_magnetic_field_covarianceList("<magnetic_field_covarianceList n=\"");
		static const Smart::KnuthMorrisPratt kmp_magnetic_field_covariance("\">");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_magnetic_field.search(is)) {
			ROSCommon_msgs::Geometry_msgs_Vector3 magnetic_fieldItem;
			magnetic_fieldItem.from_xml(is);
			setMagnetic_field(magnetic_fieldItem);
		}
		if(kmp_magnetic_field_covarianceList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			double magnetic_field_covarianceItem;
			std::vector<double> magnetic_field_covarianceList;
			kmp_magnetic_field_covariance.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_magnetic_field_covariance.search(is)) {
					is >> magnetic_field_covarianceItem;
					magnetic_field_covarianceList.push_back(magnetic_field_covarianceItem);
				}
			}
			setMagnetic_field_covariance(magnetic_field_covarianceList);
		}
	}
	
	/*
	void Sensor_msgs_MagneticFieldCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Sensor_msgs_MagneticFieldACE.hh)
		cdr << idl_Sensor_msgs_MagneticField;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Sensor_msgs_MagneticField);
		cdr << data_hash;
		// std::cout << "Sensor_msgs_MagneticFieldCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Sensor_msgs_MagneticFieldCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Sensor_msgs_MagneticFieldACE.hh)
		cdr >> idl_Sensor_msgs_MagneticField;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Sensor_msgs_MagneticField);
		assert(data_hash == own_hash);
		// std::cout << "Sensor_msgs_MagneticFieldCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
