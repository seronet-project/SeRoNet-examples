//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Nav_msgs_GetMapActionResultCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Nav_msgs_GetMapActionResultACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Nav_msgs_GetMapActionResultCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Nav_msgs_GetMapActionResultCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Nav_msgs_GetMapActionResult.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Actionlib_msgs_GoalStatus(idl_Nav_msgs_GetMapActionResult.status)
		ROSCommon_msgs::Actionlib_msgs_GoalStatus::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Nav_msgs_GetMapResult(idl_Nav_msgs_GetMapActionResult.result)
		ROSCommon_msgs::Nav_msgs_GetMapResult::getAllHashValues(hashes);
	}
	
	void Nav_msgs_GetMapActionResultCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Nav_msgs_GetMapActionResultCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Nav_msgs_GetMapActionResult.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Actionlib_msgs_GoalStatus(idl_Nav_msgs_GetMapActionResult.status)
		ROSCommon_msgs::Actionlib_msgs_GoalStatus::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Nav_msgs_GetMapResult(idl_Nav_msgs_GetMapActionResult.result)
		ROSCommon_msgs::Nav_msgs_GetMapResult::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Nav_msgs_GetMapActionResultCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		seed += ROSCommon_msgs::Actionlib_msgs_GoalStatus::generateDataHash(data.status);
		seed += ROSCommon_msgs::Nav_msgs_GetMapResult::generateDataHash(data.result);
		
		return seed;
	}
	#endif
	
	// default constructor
	Nav_msgs_GetMapActionResultCore::Nav_msgs_GetMapActionResultCore()
	:	idl_Nav_msgs_GetMapActionResult()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setStatus(ROSCommon_msgs::Actionlib_msgs_GoalStatus());
		setResult(ROSCommon_msgs::Nav_msgs_GetMapResult());
	}
	
	Nav_msgs_GetMapActionResultCore::Nav_msgs_GetMapActionResultCore(const DATATYPE &data)
	:	idl_Nav_msgs_GetMapActionResult(data)
	{  }

	Nav_msgs_GetMapActionResultCore::~Nav_msgs_GetMapActionResultCore()
	{  }
	
	void Nav_msgs_GetMapActionResultCore::to_ostream(std::ostream &os) const
	{
	  os << "Nav_msgs_GetMapActionResult(";
	  getHeader().to_ostream(os);
	  getStatus().to_ostream(os);
	  getResult().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void Nav_msgs_GetMapActionResultCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<status>";
		getStatus().to_xml(os, indent);
		os << indent << "</status>";
		os << indent << "<result>";
		getResult().to_xml(os, indent);
		os << indent << "</result>";
	}
	
	// restore from xml stream
	void Nav_msgs_GetMapActionResultCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_status("<status>");
		static const Smart::KnuthMorrisPratt kmp_result("<result>");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_status.search(is)) {
			ROSCommon_msgs::Actionlib_msgs_GoalStatus statusItem;
			statusItem.from_xml(is);
			setStatus(statusItem);
		}
		if(kmp_result.search(is)) {
			ROSCommon_msgs::Nav_msgs_GetMapResult resultItem;
			resultItem.from_xml(is);
			setResult(resultItem);
		}
	}
	
	/*
	void Nav_msgs_GetMapActionResultCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Nav_msgs_GetMapActionResultACE.hh)
		cdr << idl_Nav_msgs_GetMapActionResult;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Nav_msgs_GetMapActionResult);
		cdr << data_hash;
		// std::cout << "Nav_msgs_GetMapActionResultCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Nav_msgs_GetMapActionResultCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Nav_msgs_GetMapActionResultACE.hh)
		cdr >> idl_Nav_msgs_GetMapActionResult;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Nav_msgs_GetMapActionResult);
		assert(data_hash == own_hash);
		// std::cout << "Nav_msgs_GetMapActionResultCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
