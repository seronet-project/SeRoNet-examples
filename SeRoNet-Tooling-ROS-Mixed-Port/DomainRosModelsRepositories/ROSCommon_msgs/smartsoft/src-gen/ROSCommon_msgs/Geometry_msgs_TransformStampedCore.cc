//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Geometry_msgs_TransformStampedCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Geometry_msgs_TransformStampedACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Geometry_msgs_TransformStampedCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Geometry_msgs_TransformStampedCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Geometry_msgs_TransformStamped.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Transform(idl_Geometry_msgs_TransformStamped.transform)
		ROSCommon_msgs::Geometry_msgs_Transform::getAllHashValues(hashes);
	}
	
	void Geometry_msgs_TransformStampedCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Geometry_msgs_TransformStampedCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Geometry_msgs_TransformStamped.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Transform(idl_Geometry_msgs_TransformStamped.transform)
		ROSCommon_msgs::Geometry_msgs_Transform::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Geometry_msgs_TransformStampedCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		boost::hash_combine(seed, std::string(data.child_frame_id.c_str()));
		seed += ROSCommon_msgs::Geometry_msgs_Transform::generateDataHash(data.transform);
		
		return seed;
	}
	#endif
	
	// default constructor
	Geometry_msgs_TransformStampedCore::Geometry_msgs_TransformStampedCore()
	:	idl_Geometry_msgs_TransformStamped()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setChild_frame_id("");
		setTransform(ROSCommon_msgs::Geometry_msgs_Transform());
	}
	
	Geometry_msgs_TransformStampedCore::Geometry_msgs_TransformStampedCore(const DATATYPE &data)
	:	idl_Geometry_msgs_TransformStamped(data)
	{  }

	Geometry_msgs_TransformStampedCore::~Geometry_msgs_TransformStampedCore()
	{  }
	
	void Geometry_msgs_TransformStampedCore::to_ostream(std::ostream &os) const
	{
	  os << "Geometry_msgs_TransformStamped(";
	  getHeader().to_ostream(os);
	  os << getChild_frame_id() << " ";
	  getTransform().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void Geometry_msgs_TransformStampedCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<child_frame_id>" << getChild_frame_id() << "</child_frame_id>";
		os << indent << "<transform>";
		getTransform().to_xml(os, indent);
		os << indent << "</transform>";
	}
	
	// restore from xml stream
	void Geometry_msgs_TransformStampedCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_child_frame_id("<child_frame_id>");
		static const Smart::KnuthMorrisPratt kmp_transform("<transform>");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_child_frame_id.search(is)) {
			std::string child_frame_idItem;
			is >> child_frame_idItem;
			setChild_frame_id(child_frame_idItem);
		}
		if(kmp_transform.search(is)) {
			ROSCommon_msgs::Geometry_msgs_Transform transformItem;
			transformItem.from_xml(is);
			setTransform(transformItem);
		}
	}
	
	/*
	void Geometry_msgs_TransformStampedCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Geometry_msgs_TransformStampedACE.hh)
		cdr << idl_Geometry_msgs_TransformStamped;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Geometry_msgs_TransformStamped);
		cdr << data_hash;
		// std::cout << "Geometry_msgs_TransformStampedCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Geometry_msgs_TransformStampedCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Geometry_msgs_TransformStampedACE.hh)
		cdr >> idl_Geometry_msgs_TransformStamped;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Geometry_msgs_TransformStamped);
		assert(data_hash == own_hash);
		// std::cout << "Geometry_msgs_TransformStampedCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
