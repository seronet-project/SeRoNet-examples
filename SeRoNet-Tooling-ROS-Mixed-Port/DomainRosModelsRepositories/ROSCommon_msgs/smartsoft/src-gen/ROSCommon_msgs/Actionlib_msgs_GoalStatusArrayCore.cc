//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Actionlib_msgs_GoalStatusArrayCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Actionlib_msgs_GoalStatusArrayACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Actionlib_msgs_GoalStatusArrayCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Actionlib_msgs_GoalStatusArrayCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Actionlib_msgs_GoalStatusArray.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Actionlib_msgs_GoalStatus(idl_Actionlib_msgs_GoalStatusArray.status_list)
		ROSCommon_msgs::Actionlib_msgs_GoalStatus::getAllHashValues(hashes);
	}
	
	void Actionlib_msgs_GoalStatusArrayCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Actionlib_msgs_GoalStatusArrayCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Actionlib_msgs_GoalStatusArray.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Actionlib_msgs_GoalStatus(idl_Actionlib_msgs_GoalStatusArray.status_list)
		ROSCommon_msgs::Actionlib_msgs_GoalStatus::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Actionlib_msgs_GoalStatusArrayCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		std::vector<ROSCommon_msgsIDL::Actionlib_msgs_GoalStatus>::const_iterator data_status_listIt;
		for(data_status_listIt=data.status_list.begin(); data_status_listIt!=data.status_list.end(); data_status_listIt++) {
			seed += ROSCommon_msgs::Actionlib_msgs_GoalStatus::generateDataHash(*data_status_listIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Actionlib_msgs_GoalStatusArrayCore::Actionlib_msgs_GoalStatusArrayCore()
	:	idl_Actionlib_msgs_GoalStatusArray()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setStatus_list(std::vector<ROSCommon_msgs::Actionlib_msgs_GoalStatus>());
	}
	
	Actionlib_msgs_GoalStatusArrayCore::Actionlib_msgs_GoalStatusArrayCore(const DATATYPE &data)
	:	idl_Actionlib_msgs_GoalStatusArray(data)
	{  }

	Actionlib_msgs_GoalStatusArrayCore::~Actionlib_msgs_GoalStatusArrayCore()
	{  }
	
	void Actionlib_msgs_GoalStatusArrayCore::to_ostream(std::ostream &os) const
	{
	  os << "Actionlib_msgs_GoalStatusArray(";
	  getHeader().to_ostream(os);
	  std::vector<ROSCommon_msgs::Actionlib_msgs_GoalStatus>::const_iterator status_listIt;
	  std::vector<ROSCommon_msgs::Actionlib_msgs_GoalStatus> status_listList = getStatus_listCopy();
	  for(status_listIt=status_listList.begin(); status_listIt!=status_listList.end(); status_listIt++) {
	  	status_listIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Actionlib_msgs_GoalStatusArrayCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		std::vector<ROSCommon_msgs::Actionlib_msgs_GoalStatus>::const_iterator status_listIt;
		std::vector<ROSCommon_msgs::Actionlib_msgs_GoalStatus> status_listList = getStatus_listCopy();
		counter = 0;
		os << indent << "<status_listList n=\"" << status_listList.size() << "\">";
		for(status_listIt=status_listList.begin(); status_listIt!=status_listList.end(); status_listIt++) {
			os << indent << "<status_list i=\"" << counter++ << "\">";
			status_listIt->to_xml(os, indent);
			os << indent << "</status_list>";
		}
		os << indent << "</status_listList>";
	}
	
	// restore from xml stream
	void Actionlib_msgs_GoalStatusArrayCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_status_listList("<status_listList n=\"");
		static const Smart::KnuthMorrisPratt kmp_status_list("\">");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_status_listList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Actionlib_msgs_GoalStatus status_listItem;
			std::vector<ROSCommon_msgs::Actionlib_msgs_GoalStatus> status_listList;
			kmp_status_list.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_status_list.search(is)) {
					status_listItem.from_xml(is);
					status_listList.push_back(status_listItem);
				}
			}
			setStatus_list(status_listList);
		}
	}
	
	/*
	void Actionlib_msgs_GoalStatusArrayCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Actionlib_msgs_GoalStatusArrayACE.hh)
		cdr << idl_Actionlib_msgs_GoalStatusArray;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Actionlib_msgs_GoalStatusArray);
		cdr << data_hash;
		// std::cout << "Actionlib_msgs_GoalStatusArrayCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Actionlib_msgs_GoalStatusArrayCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Actionlib_msgs_GoalStatusArrayACE.hh)
		cdr >> idl_Actionlib_msgs_GoalStatusArray;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Actionlib_msgs_GoalStatusArray);
		assert(data_hash == own_hash);
		// std::cout << "Actionlib_msgs_GoalStatusArrayCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
