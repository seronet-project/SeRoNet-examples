//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Visualization_msgs_ImageMarkerCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Visualization_msgs_ImageMarkerACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Visualization_msgs_ImageMarkerCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Visualization_msgs_ImageMarkerCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for ROSRos_core::Std_msgs_Header(idl_Visualization_msgs_ImageMarker.header)
		ROSRos_core::Std_msgs_Header::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Visualization_msgs_ImageMarker.position)
		ROSCommon_msgs::Geometry_msgs_Point::getAllHashValues(hashes);
		// get hash value(s) for ROSRos_core::Std_msgs_ColorRGBA(idl_Visualization_msgs_ImageMarker.outline_color)
		ROSRos_core::Std_msgs_ColorRGBA::getAllHashValues(hashes);
		// get hash value(s) for ROSRos_core::Std_msgs_ColorRGBA(idl_Visualization_msgs_ImageMarker.fill_color)
		ROSRos_core::Std_msgs_ColorRGBA::getAllHashValues(hashes);
		// get hash value(s) for CommBasicObjects::CommTimeStamp(idl_Visualization_msgs_ImageMarker.lifetime)
		CommBasicObjects::CommTimeStamp::getAllHashValues(hashes);
		// get hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Visualization_msgs_ImageMarker.points)
		ROSCommon_msgs::Geometry_msgs_Point::getAllHashValues(hashes);
		// get hash value(s) for ROSRos_core::Std_msgs_ColorRGBA(idl_Visualization_msgs_ImageMarker.outline_colors)
		ROSRos_core::Std_msgs_ColorRGBA::getAllHashValues(hashes);
	}
	
	void Visualization_msgs_ImageMarkerCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Visualization_msgs_ImageMarkerCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for ROSRos_core::Std_msgs_Header(idl_Visualization_msgs_ImageMarker.header)
		ROSRos_core::Std_msgs_Header::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Visualization_msgs_ImageMarker.position)
		ROSCommon_msgs::Geometry_msgs_Point::checkAllHashValues(hashes);
		// check hash value(s) for ROSRos_core::Std_msgs_ColorRGBA(idl_Visualization_msgs_ImageMarker.outline_color)
		ROSRos_core::Std_msgs_ColorRGBA::checkAllHashValues(hashes);
		// check hash value(s) for ROSRos_core::Std_msgs_ColorRGBA(idl_Visualization_msgs_ImageMarker.fill_color)
		ROSRos_core::Std_msgs_ColorRGBA::checkAllHashValues(hashes);
		// check hash value(s) for CommBasicObjects::CommTimeStamp(idl_Visualization_msgs_ImageMarker.lifetime)
		CommBasicObjects::CommTimeStamp::checkAllHashValues(hashes);
		// check hash value(s) for ROSCommon_msgs::Geometry_msgs_Point(idl_Visualization_msgs_ImageMarker.points)
		ROSCommon_msgs::Geometry_msgs_Point::checkAllHashValues(hashes);
		// check hash value(s) for ROSRos_core::Std_msgs_ColorRGBA(idl_Visualization_msgs_ImageMarker.outline_colors)
		ROSRos_core::Std_msgs_ColorRGBA::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t Visualization_msgs_ImageMarkerCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		seed += ROSRos_core::Std_msgs_Header::generateDataHash(data.header);
		boost::hash_combine(seed, std::string(data.ns.c_str()));
		boost::hash_combine(seed, data.id);
		boost::hash_combine(seed, data.type);
		boost::hash_combine(seed, data.action);
		seed += ROSCommon_msgs::Geometry_msgs_Point::generateDataHash(data.position);
		boost::hash_combine(seed, data.scale);
		seed += ROSRos_core::Std_msgs_ColorRGBA::generateDataHash(data.outline_color);
		boost::hash_combine(seed, data.filled);
		seed += ROSRos_core::Std_msgs_ColorRGBA::generateDataHash(data.fill_color);
		seed += CommBasicObjects::CommTimeStamp::generateDataHash(data.lifetime);
		std::vector<ROSCommon_msgsIDL::Geometry_msgs_Point>::const_iterator data_pointsIt;
		for(data_pointsIt=data.points.begin(); data_pointsIt!=data.points.end(); data_pointsIt++) {
			seed += ROSCommon_msgs::Geometry_msgs_Point::generateDataHash(*data_pointsIt);
		}
		std::vector<ROSRos_coreIDL::Std_msgs_ColorRGBA>::const_iterator data_outline_colorsIt;
		for(data_outline_colorsIt=data.outline_colors.begin(); data_outline_colorsIt!=data.outline_colors.end(); data_outline_colorsIt++) {
			seed += ROSRos_core::Std_msgs_ColorRGBA::generateDataHash(*data_outline_colorsIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Visualization_msgs_ImageMarkerCore::Visualization_msgs_ImageMarkerCore()
	:	idl_Visualization_msgs_ImageMarker()
	{  
		setHeader(ROSRos_core::Std_msgs_Header());
		setNs("");
		setId(0);
		setType(0);
		setAction(0);
		setPosition(ROSCommon_msgs::Geometry_msgs_Point());
		setScale(0.0);
		setOutline_color(ROSRos_core::Std_msgs_ColorRGBA());
		setFilled(0);
		setFill_color(ROSRos_core::Std_msgs_ColorRGBA());
		setLifetime(CommBasicObjects::CommTimeStamp());
		setPoints(std::vector<ROSCommon_msgs::Geometry_msgs_Point>());
		setOutline_colors(std::vector<ROSRos_core::Std_msgs_ColorRGBA>());
	}
	
	Visualization_msgs_ImageMarkerCore::Visualization_msgs_ImageMarkerCore(const DATATYPE &data)
	:	idl_Visualization_msgs_ImageMarker(data)
	{  }

	Visualization_msgs_ImageMarkerCore::~Visualization_msgs_ImageMarkerCore()
	{  }
	
	void Visualization_msgs_ImageMarkerCore::to_ostream(std::ostream &os) const
	{
	  os << "Visualization_msgs_ImageMarker(";
	  getHeader().to_ostream(os);
	  os << getNs() << " ";
	  os << getId() << " ";
	  os << getType() << " ";
	  os << getAction() << " ";
	  getPosition().to_ostream(os);
	  os << getScale() << " ";
	  getOutline_color().to_ostream(os);
	  os << getFilled() << " ";
	  getFill_color().to_ostream(os);
	  getLifetime().to_ostream(os);
	  std::vector<ROSCommon_msgs::Geometry_msgs_Point>::const_iterator pointsIt;
	  std::vector<ROSCommon_msgs::Geometry_msgs_Point> pointsList = getPointsCopy();
	  for(pointsIt=pointsList.begin(); pointsIt!=pointsList.end(); pointsIt++) {
	  	pointsIt->to_ostream(os);
	  }
	  std::vector<ROSRos_core::Std_msgs_ColorRGBA>::const_iterator outline_colorsIt;
	  std::vector<ROSRos_core::Std_msgs_ColorRGBA> outline_colorsList = getOutline_colorsCopy();
	  for(outline_colorsIt=outline_colorsList.begin(); outline_colorsIt!=outline_colorsList.end(); outline_colorsIt++) {
	  	outline_colorsIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Visualization_msgs_ImageMarkerCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<header>";
		getHeader().to_xml(os, indent);
		os << indent << "</header>";
		os << indent << "<ns>" << getNs() << "</ns>";
		os << indent << "<id>" << getId() << "</id>";
		os << indent << "<type>" << getType() << "</type>";
		os << indent << "<action>" << getAction() << "</action>";
		os << indent << "<position>";
		getPosition().to_xml(os, indent);
		os << indent << "</position>";
		os << indent << "<scale>" << getScale() << "</scale>";
		os << indent << "<outline_color>";
		getOutline_color().to_xml(os, indent);
		os << indent << "</outline_color>";
		os << indent << "<filled>" << getFilled() << "</filled>";
		os << indent << "<fill_color>";
		getFill_color().to_xml(os, indent);
		os << indent << "</fill_color>";
		os << indent << "<lifetime>";
		getLifetime().to_xml(os, indent);
		os << indent << "</lifetime>";
		std::vector<ROSCommon_msgs::Geometry_msgs_Point>::const_iterator pointsIt;
		std::vector<ROSCommon_msgs::Geometry_msgs_Point> pointsList = getPointsCopy();
		counter = 0;
		os << indent << "<pointsList n=\"" << pointsList.size() << "\">";
		for(pointsIt=pointsList.begin(); pointsIt!=pointsList.end(); pointsIt++) {
			os << indent << "<points i=\"" << counter++ << "\">";
			pointsIt->to_xml(os, indent);
			os << indent << "</points>";
		}
		os << indent << "</pointsList>";
		std::vector<ROSRos_core::Std_msgs_ColorRGBA>::const_iterator outline_colorsIt;
		std::vector<ROSRos_core::Std_msgs_ColorRGBA> outline_colorsList = getOutline_colorsCopy();
		counter = 0;
		os << indent << "<outline_colorsList n=\"" << outline_colorsList.size() << "\">";
		for(outline_colorsIt=outline_colorsList.begin(); outline_colorsIt!=outline_colorsList.end(); outline_colorsIt++) {
			os << indent << "<outline_colors i=\"" << counter++ << "\">";
			outline_colorsIt->to_xml(os, indent);
			os << indent << "</outline_colors>";
		}
		os << indent << "</outline_colorsList>";
	}
	
	// restore from xml stream
	void Visualization_msgs_ImageMarkerCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_header("<header>");
		static const Smart::KnuthMorrisPratt kmp_ns("<ns>");
		static const Smart::KnuthMorrisPratt kmp_id("<id>");
		static const Smart::KnuthMorrisPratt kmp_type("<type>");
		static const Smart::KnuthMorrisPratt kmp_action("<action>");
		static const Smart::KnuthMorrisPratt kmp_position("<position>");
		static const Smart::KnuthMorrisPratt kmp_scale("<scale>");
		static const Smart::KnuthMorrisPratt kmp_outline_color("<outline_color>");
		static const Smart::KnuthMorrisPratt kmp_filled("<filled>");
		static const Smart::KnuthMorrisPratt kmp_fill_color("<fill_color>");
		static const Smart::KnuthMorrisPratt kmp_lifetime("<lifetime>");
		static const Smart::KnuthMorrisPratt kmp_pointsList("<pointsList n=\"");
		static const Smart::KnuthMorrisPratt kmp_points("\">");
		static const Smart::KnuthMorrisPratt kmp_outline_colorsList("<outline_colorsList n=\"");
		static const Smart::KnuthMorrisPratt kmp_outline_colors("\">");
		
		if(kmp_header.search(is)) {
			ROSRos_core::Std_msgs_Header headerItem;
			headerItem.from_xml(is);
			setHeader(headerItem);
		}
		if(kmp_ns.search(is)) {
			std::string nsItem;
			is >> nsItem;
			setNs(nsItem);
		}
		if(kmp_id.search(is)) {
			int idItem;
			is >> idItem;
			setId(idItem);
		}
		if(kmp_type.search(is)) {
			int typeItem;
			is >> typeItem;
			setType(typeItem);
		}
		if(kmp_action.search(is)) {
			int actionItem;
			is >> actionItem;
			setAction(actionItem);
		}
		if(kmp_position.search(is)) {
			ROSCommon_msgs::Geometry_msgs_Point positionItem;
			positionItem.from_xml(is);
			setPosition(positionItem);
		}
		if(kmp_scale.search(is)) {
			float scaleItem;
			is >> scaleItem;
			setScale(scaleItem);
		}
		if(kmp_outline_color.search(is)) {
			ROSRos_core::Std_msgs_ColorRGBA outline_colorItem;
			outline_colorItem.from_xml(is);
			setOutline_color(outline_colorItem);
		}
		if(kmp_filled.search(is)) {
			unsigned char filledItem;
			is >> filledItem;
			setFilled(filledItem);
		}
		if(kmp_fill_color.search(is)) {
			ROSRos_core::Std_msgs_ColorRGBA fill_colorItem;
			fill_colorItem.from_xml(is);
			setFill_color(fill_colorItem);
		}
		if(kmp_lifetime.search(is)) {
			CommBasicObjects::CommTimeStamp lifetimeItem;
			lifetimeItem.from_xml(is);
			setLifetime(lifetimeItem);
		}
		if(kmp_pointsList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSCommon_msgs::Geometry_msgs_Point pointsItem;
			std::vector<ROSCommon_msgs::Geometry_msgs_Point> pointsList;
			kmp_points.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_points.search(is)) {
					pointsItem.from_xml(is);
					pointsList.push_back(pointsItem);
				}
			}
			setPoints(pointsList);
		}
		if(kmp_outline_colorsList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			ROSRos_core::Std_msgs_ColorRGBA outline_colorsItem;
			std::vector<ROSRos_core::Std_msgs_ColorRGBA> outline_colorsList;
			kmp_outline_colors.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_outline_colors.search(is)) {
					outline_colorsItem.from_xml(is);
					outline_colorsList.push_back(outline_colorsItem);
				}
			}
			setOutline_colors(outline_colorsList);
		}
	}
	
	/*
	void Visualization_msgs_ImageMarkerCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Visualization_msgs_ImageMarkerACE.hh)
		cdr << idl_Visualization_msgs_ImageMarker;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Visualization_msgs_ImageMarker);
		cdr << data_hash;
		// std::cout << "Visualization_msgs_ImageMarkerCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Visualization_msgs_ImageMarkerCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Visualization_msgs_ImageMarkerACE.hh)
		cdr >> idl_Visualization_msgs_ImageMarker;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Visualization_msgs_ImageMarker);
		assert(data_hash == own_hash);
		// std::cout << "Visualization_msgs_ImageMarkerCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
