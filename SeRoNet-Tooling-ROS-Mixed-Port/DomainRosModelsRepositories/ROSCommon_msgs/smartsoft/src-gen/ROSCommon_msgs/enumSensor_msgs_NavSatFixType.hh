//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef ROSCOMMON_MSGS_SENSOR_MSGS_NAVSATFIXTYPE_ENUM_H_
#define ROSCOMMON_MSGS_SENSOR_MSGS_NAVSATFIXTYPE_ENUM_H_

#include <ROSCommon_msgs/enumSensor_msgs_NavSatFixTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace ROSCommon_msgs {
	
	class Sensor_msgs_NavSatFixType {
	private:
		ROSCommon_msgsIDL::Sensor_msgs_NavSatFixType value;
		
	public:
		enum ENUM_Sensor_msgs_NavSatFixType {
			ENUM_VALUE_UNDEFINED = 0,
			COVARIANCE_TYPE_UNKNOWN = 1,
			COVARIANCE_TYPE_APPROXIMATED = 2,
			COVARIANCE_TYPE_DIAGONAL_KNOWN = 3,
			COVARIANCE_TYPE_KNOWN = 4
		};
		
		// default constructor
		Sensor_msgs_NavSatFixType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		Sensor_msgs_NavSatFixType(ENUM_Sensor_msgs_NavSatFixType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		Sensor_msgs_NavSatFixType(ROSCommon_msgsIDL::Sensor_msgs_NavSatFixType e) {
			value = e;
		}
		
		//const ROSCommon_msgsIDL::Sensor_msgs_NavSatFixType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_Sensor_msgs_NavSatFixType() const {
			return static_cast<ENUM_Sensor_msgs_NavSatFixType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_Sensor_msgs_NavSatFixType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case COVARIANCE_TYPE_UNKNOWN:
					result = "Sensor_msgs_NavSatFixType::COVARIANCE_TYPE_UNKNOWN";
					break;
				case COVARIANCE_TYPE_APPROXIMATED:
					result = "Sensor_msgs_NavSatFixType::COVARIANCE_TYPE_APPROXIMATED";
					break;
				case COVARIANCE_TYPE_DIAGONAL_KNOWN:
					result = "Sensor_msgs_NavSatFixType::COVARIANCE_TYPE_DIAGONAL_KNOWN";
					break;
				case COVARIANCE_TYPE_KNOWN:
					result = "Sensor_msgs_NavSatFixType::COVARIANCE_TYPE_KNOWN";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const Sensor_msgs_NavSatFixType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
