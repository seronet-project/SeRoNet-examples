//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Shape_msgs_SolidPrimitiveCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Shape_msgs_SolidPrimitiveACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Shape_msgs_SolidPrimitiveCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Shape_msgs_SolidPrimitiveCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
	}
	
	void Shape_msgs_SolidPrimitiveCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Shape_msgs_SolidPrimitiveCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
	}
	
	#ifdef ENABLE_HASH
	size_t Shape_msgs_SolidPrimitiveCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.type);
		std::vector<ACE_CDR::Double>::const_iterator data_dimensionsIt;
		for(data_dimensionsIt=data.dimensions.begin(); data_dimensionsIt!=data.dimensions.end(); data_dimensionsIt++) {
			boost::hash_combine(seed, *data_dimensionsIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	Shape_msgs_SolidPrimitiveCore::Shape_msgs_SolidPrimitiveCore()
	:	idl_Shape_msgs_SolidPrimitive()
	{  
		setType(0);
		setDimensions(std::vector<double>());
	}
	
	Shape_msgs_SolidPrimitiveCore::Shape_msgs_SolidPrimitiveCore(const DATATYPE &data)
	:	idl_Shape_msgs_SolidPrimitive(data)
	{  }

	Shape_msgs_SolidPrimitiveCore::~Shape_msgs_SolidPrimitiveCore()
	{  }
	
	void Shape_msgs_SolidPrimitiveCore::to_ostream(std::ostream &os) const
	{
	  os << "Shape_msgs_SolidPrimitive(";
	  os << getType() << " ";
	  std::vector<double>::const_iterator dimensionsIt;
	  std::vector<double> dimensionsList = getDimensionsCopy();
	  for(dimensionsIt=dimensionsList.begin(); dimensionsIt!=dimensionsList.end(); dimensionsIt++) {
	  	os << *dimensionsIt << " ";
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void Shape_msgs_SolidPrimitiveCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		os << indent << "<type>" << getType() << "</type>";
		std::vector<double>::const_iterator dimensionsIt;
		std::vector<double> dimensionsList = getDimensionsCopy();
		counter = 0;
		os << indent << "<dimensionsList n=\"" << dimensionsList.size() << "\">";
		for(dimensionsIt=dimensionsList.begin(); dimensionsIt!=dimensionsList.end(); dimensionsIt++) {
			os << indent << "<dimensions i=\"" << counter++ << "\">" << *dimensionsIt << "</dimensions>";
		}
		os << indent << "</dimensionsList>";
	}
	
	// restore from xml stream
	void Shape_msgs_SolidPrimitiveCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_type("<type>");
		static const Smart::KnuthMorrisPratt kmp_dimensionsList("<dimensionsList n=\"");
		static const Smart::KnuthMorrisPratt kmp_dimensions("\">");
		
		if(kmp_type.search(is)) {
			unsigned char typeItem;
			is >> typeItem;
			setType(typeItem);
		}
		if(kmp_dimensionsList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			double dimensionsItem;
			std::vector<double> dimensionsList;
			kmp_dimensions.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_dimensions.search(is)) {
					is >> dimensionsItem;
					dimensionsList.push_back(dimensionsItem);
				}
			}
			setDimensions(dimensionsList);
		}
	}
	
	/*
	void Shape_msgs_SolidPrimitiveCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Shape_msgs_SolidPrimitiveACE.hh)
		cdr << idl_Shape_msgs_SolidPrimitive;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Shape_msgs_SolidPrimitive);
		cdr << data_hash;
		// std::cout << "Shape_msgs_SolidPrimitiveCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Shape_msgs_SolidPrimitiveCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Shape_msgs_SolidPrimitiveACE.hh)
		cdr >> idl_Shape_msgs_SolidPrimitive;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Shape_msgs_SolidPrimitive);
		assert(data_hash == own_hash);
		// std::cout << "Shape_msgs_SolidPrimitiveCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
