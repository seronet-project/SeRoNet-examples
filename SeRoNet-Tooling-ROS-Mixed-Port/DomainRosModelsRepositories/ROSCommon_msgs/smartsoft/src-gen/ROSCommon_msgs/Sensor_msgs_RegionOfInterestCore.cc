//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ROSCommon_msgs/Sensor_msgs_RegionOfInterestCore.hh"

// serialization/deserialization operators
//#include "ROSCommon_msgs/Sensor_msgs_RegionOfInterestACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace ROSCommon_msgs 
{
	const char* Sensor_msgs_RegionOfInterestCore::getCompiledHash()
	{
		return ROSCommon_msgsIDL::REPO_HASH;
	}
	
	void Sensor_msgs_RegionOfInterestCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
	}
	
	void Sensor_msgs_RegionOfInterestCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "Sensor_msgs_RegionOfInterestCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
	}
	
	#ifdef ENABLE_HASH
	size_t Sensor_msgs_RegionOfInterestCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.x_offset);
		boost::hash_combine(seed, data.y_offset);
		boost::hash_combine(seed, data.height);
		boost::hash_combine(seed, data.width);
		boost::hash_combine(seed, data.do_rectify);
		
		return seed;
	}
	#endif
	
	// default constructor
	Sensor_msgs_RegionOfInterestCore::Sensor_msgs_RegionOfInterestCore()
	:	idl_Sensor_msgs_RegionOfInterest()
	{  
		setX_offset(0);
		setY_offset(0);
		setHeight(0);
		setWidth(0);
		setDo_rectify(false);
	}
	
	Sensor_msgs_RegionOfInterestCore::Sensor_msgs_RegionOfInterestCore(const DATATYPE &data)
	:	idl_Sensor_msgs_RegionOfInterest(data)
	{  }

	Sensor_msgs_RegionOfInterestCore::~Sensor_msgs_RegionOfInterestCore()
	{  }
	
	void Sensor_msgs_RegionOfInterestCore::to_ostream(std::ostream &os) const
	{
	  os << "Sensor_msgs_RegionOfInterest(";
	  os << getX_offset() << " ";
	  os << getY_offset() << " ";
	  os << getHeight() << " ";
	  os << getWidth() << " ";
	  os << getDo_rectify() << " ";
	  os << ") ";
	}
	
	// convert to xml stream
	void Sensor_msgs_RegionOfInterestCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<x_offset>" << getX_offset() << "</x_offset>";
		os << indent << "<y_offset>" << getY_offset() << "</y_offset>";
		os << indent << "<height>" << getHeight() << "</height>";
		os << indent << "<width>" << getWidth() << "</width>";
		os << indent << "<do_rectify>" << getDo_rectify() << "</do_rectify>";
	}
	
	// restore from xml stream
	void Sensor_msgs_RegionOfInterestCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_x_offset("<x_offset>");
		static const Smart::KnuthMorrisPratt kmp_y_offset("<y_offset>");
		static const Smart::KnuthMorrisPratt kmp_height("<height>");
		static const Smart::KnuthMorrisPratt kmp_width("<width>");
		static const Smart::KnuthMorrisPratt kmp_do_rectify("<do_rectify>");
		
		if(kmp_x_offset.search(is)) {
			unsigned int x_offsetItem;
			is >> x_offsetItem;
			setX_offset(x_offsetItem);
		}
		if(kmp_y_offset.search(is)) {
			unsigned int y_offsetItem;
			is >> y_offsetItem;
			setY_offset(y_offsetItem);
		}
		if(kmp_height.search(is)) {
			unsigned int heightItem;
			is >> heightItem;
			setHeight(heightItem);
		}
		if(kmp_width.search(is)) {
			unsigned int widthItem;
			is >> widthItem;
			setWidth(widthItem);
		}
		if(kmp_do_rectify.search(is)) {
			bool do_rectifyItem;
			is >> do_rectifyItem;
			setDo_rectify(do_rectifyItem);
		}
	}
	
	/*
	void Sensor_msgs_RegionOfInterestCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		ROSCommon_msgsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(ROSCommon_msgsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see Sensor_msgs_RegionOfInterestACE.hh)
		cdr << idl_Sensor_msgs_RegionOfInterest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_Sensor_msgs_RegionOfInterest);
		cdr << data_hash;
		// std::cout << "Sensor_msgs_RegionOfInterestCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void Sensor_msgs_RegionOfInterestCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		ROSCommon_msgsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see Sensor_msgs_RegionOfInterestACE.hh)
		cdr >> idl_Sensor_msgs_RegionOfInterest;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_Sensor_msgs_RegionOfInterest);
		assert(data_hash == own_hash);
		// std::cout << "Sensor_msgs_RegionOfInterestCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace ROSCommon_msgs */
