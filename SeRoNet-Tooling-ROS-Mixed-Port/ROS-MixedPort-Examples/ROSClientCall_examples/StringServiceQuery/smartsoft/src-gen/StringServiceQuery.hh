//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _STRINGSERVICEQUERY_HH
#define _STRINGSERVICEQUERY_HH

#include <map>
#include <iostream>
#include "aceSmartSoft.hh"
#include "smartQueryServerTaskTrigger_T.h"
#include "StringServiceQueryCore.hh"

#include "StringServiceQueryPortFactoryInterface.hh"
#include "StringServiceQueryExtension.hh"

// forward declarations
class StringServiceQueryPortFactoryInterface;
class StringServiceQueryExtension;

// includes for OpcUaBackendComponentGeneratorExtension

// includes for PlainOpcUaStringServiceQueryExtension
// include plain OPC UA device clients
// include plain OPC UA status servers

// includes for StringServiceQueryROSExtension


// include communication objects
#include <ROSRos_core/Std_srvs_SetBoolRequest.hh>
#include <ROSRos_core/Std_srvs_SetBoolRequestACE.hh>
#include <ROSRos_core/Std_srvs_SetBoolResponse.hh>
#include <ROSRos_core/Std_srvs_SetBoolResponseACE.hh>

// include tasks
// include UpcallManagers

// include input-handler(s)
// include request-handler(s)
#include "SetBoolQueryServiceAnswHandler.hh"

// include handler
#include "CompHandler.hh"


#include "SmartStateChangeHandler.hh"

#define COMP StringServiceQuery::instance()

class StringServiceQuery : public StringServiceQueryCore {
private:
	static StringServiceQuery *_stringServiceQuery;
	
	// constructor
	StringServiceQuery();
	
	// copy-constructor
	StringServiceQuery(const StringServiceQuery& cc);
	
	// destructor
	~StringServiceQuery() { };
	
	// load parameter from ini file
	void loadParameter(int argc, char* argv[]);
	
	// instantiate comp-handler
	CompHandler compHandler;
	
	// helper method that maps a string-name to an according TaskTriggerSubject
	Smart::TaskTriggerSubject* getInputTaskTriggerFromString(const std::string &client);
	
	// internal map storing the different port-creation factories (that internally map to specific middleware implementations)
	std::map<std::string, StringServiceQueryPortFactoryInterface*> portFactoryRegistry;
	
	// internal map storing various extensions of this component class
	std::map<std::string, StringServiceQueryExtension*> componentExtensionRegistry;
	
public:
	
	// define tasks
	
	// define input-ports
	
	// define request-ports
	
	// define input-handler
	
	// define output-ports
	
	// define answer-ports
	Smart::IQueryServerPattern<ROSRos_core::Std_srvs_SetBoolRequest, ROSRos_core::Std_srvs_SetBoolResponse> *setBoolQueryServiceAnsw;
	Smart::QueryServerTaskTrigger<ROSRos_core::Std_srvs_SetBoolRequest, ROSRos_core::Std_srvs_SetBoolResponse> *setBoolQueryServiceAnswInputTaskTrigger;
	
	// define request-handlers
	SetBoolQueryServiceAnswHandler *setBoolQueryServiceAnswHandler;
	
	// definitions of OpcUaBackendComponentGeneratorExtension
	
	// definitions of PlainOpcUaStringServiceQueryExtension
	
	// definitions of StringServiceQueryROSExtension
	
	
	// define default slave ports
	SmartACE::StateSlave *stateSlave;
	SmartStateChangeHandler *stateChangeHandler;
	SmartACE::WiringSlave *wiringSlave;
	
	
	/// this method is used to register different PortFactory classes (one for each supported middleware framework)
	void addPortFactory(const std::string &name, StringServiceQueryPortFactoryInterface *portFactory);
	
	SmartACE::SmartComponent* getComponentImpl();
	
	/// this method is used to register different component-extension classes
	void addExtension(StringServiceQueryExtension *extension);
	
	/// this method allows to access the registered component-extensions (automatically converting to the actuall implementation type)
	template <typename T>
	T* getExtension(const std::string &name) {
		auto it = componentExtensionRegistry.find(name);
		if(it != componentExtensionRegistry.end()) {
			return dynamic_cast<T*>(it->second);
		}
		return 0;
	}
	
	/// initialize component's internal members
	void init(int argc, char *argv[]);
	
	/// execute the component's infrastructure
	void run();
	
	/// clean-up component's resources
	void fini();
	
	/// call this method to set the overall component into the Alive state (i.e. component is then ready to operate)
	void setStartupFinished();
	
	/// connect all component's client ports
	Smart::StatusCode connectAndStartAllServices();
	
	/// start all assocuated Activities
	void startAllTasks();
	
	/// start all associated timers
	void startAllTimers();
	

	// return singleton instance
	static StringServiceQuery* instance()
	{
		if(_stringServiceQuery == 0) {
			_stringServiceQuery = new StringServiceQuery();
		}
		return _stringServiceQuery;
	}
	
	static void deleteInstance() {
		if(_stringServiceQuery != 0) {
			delete _stringServiceQuery;
		}
	}
	
	// connections parameter
	struct connections_struct
	{
		// component struct
		struct component_struct
		{
			// the name of the component
			std::string name;
			std::string initialComponentMode;
			std::string defaultScheduler;
			bool useLogger;
		} component;
		
		//--- task parameter ---
		
		//--- upcall parameter ---
		
		//--- server port parameter ---
		struct SetBoolQueryServiceAnsw_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} setBoolQueryServiceAnsw;
	
		//--- client port parameter ---
		
		// -- parameters for OpcUaBackendComponentGeneratorExtension
		
		// -- parameters for PlainOpcUaStringServiceQueryExtension
		
		// -- parameters for StringServiceQueryROSExtension
		
	} connections;
};
#endif
